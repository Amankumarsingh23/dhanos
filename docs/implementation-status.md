# DhanOS — Implementation Status

Last updated: 2026-07-23.

## 0. Baseline

The engineering foundation phase is complete. Framework, tooling, typed env validation, Supabase client plumbing, shared UI components, and app routing scaffolding are in place — see section 1. No financial modules (accounts, transactions, investments, etc.) exist yet; those begin with phase 1 of the recommended order in section 3.

## 1. What exists today

| Item | Status |
|---|---|
| Git repository | Initialized |
| Framework/package manager | Next.js 16 (App Router) + TypeScript, pnpm |
| Strict TypeScript | Enabled (`strict` + `noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`) |
| Linting/formatting | ESLint (flat config, `eslint-config-next` + `eslint-config-prettier`) and Prettier (with `prettier-plugin-tailwindcss`) |
| Import aliases | `@/*` → `src/*` |
| Typed environment validation | `src/lib/env/client.ts` (public vars) and `src/lib/env/server.ts` (secrets, guarded by the `server-only` package — verified to fail the build if imported from a Client Component); `.env.example` tracked, `.env.local` gitignored |
| Supabase project | Local dev stack only (`supabase/config.toml`, Docker via `pnpm db:start`) — no remote/hosted project created or linked yet |
| Database schema/migrations | 32 migrations implemented (24 through PROMPT 14, 6 for PROMPT 16's investment domain, 1 for PROMPT 17's SIPs, and 1 for PROMPT 19's staking positions — see the Investments/SIPs/Staking rows below for those): `set_updated_at()` trigger utility, `households`/`household_memberships` (tenancy + RLS, roles owner/admin/editor/viewer, status active/invited/suspended, idempotent `get_or_create_household()` RPC), `net_worth_snapshots` (append-only), `profiles` (per-user preferences + RLS + `handle_new_user` auto-provisioning trigger), the full core ledger schema — `people`, `institutions`, `financial_accounts`, `account_balance_snapshots` (append-only), `transaction_categories`, `recurring_rules`, `transactions` (kind-discriminated, household/currency-consistency trigger, `cash_flow_transactions` reporting view), `transaction_splits` (deferred constraint trigger enforcing split totals), `attachments` (polymorphic, trigger-validated), `activity_events` (append-only audit log) — `record_account_balance_correction()` (SECURITY INVOKER RPC atomically writing a balance snapshot + adjustment transaction pair — see PROMPT 9) — (PROMPT 10) `transactions.status` grown to the full `planned`/`pending`/`cleared`/`cancelled`/`reconciled` lifecycle, `transaction_categories` grown `classification`/`sort_order`/`is_system_default`, `seed_default_transaction_categories()` (auto-seeds the PROMPT 10 default taxonomy for every household via a `households` insert trigger, plus a one-time backfill), and `create_transaction_with_splits()`/`update_transaction_with_splits()` (SECURITY INVOKER RPCs writing a transaction and its `transaction_splits` atomically) — (PROMPT 11) `income_sources` (declared, expected income streams — household/currency-consistency trigger) plus `transactions.income_source_id` (nullable, only valid when `kind = 'income'`, folded into `check_transaction_consistency()`) — (PROMPT 12) `transactions.is_planned` (budgeting-intent flag, distinct from `status`'s planned/pending/cleared lifecycle), `create_transaction_with_splits()`/`update_transaction_with_splits()` grown a `p_is_planned` parameter, and a private `documents` Storage bucket + household-scoped RLS on `storage.objects` (keyed off the household id as the first path segment) — the first working receipt/attachment upload path against the PROMPT 9 `attachments` table — and (PROMPT 13) `transactions.transfer_fee_minor_units`/`transfer_destination_amount_minor_units`/`exchange_rate` (a transfer fee debits the source account only; a cross-currency transfer requires both the converted amount and the rate together, enforced by `check_transaction_consistency()`, never invented by the app), `transactions_reversal_shape` (replaces `transactions_refund_shape` — a `kind = transfer` row may now also set `reverses_transaction_id`, pointing at another transfer, alongside the existing refund-reverses-expense rule), `create_transaction_with_splits()`/`update_transaction_with_splits()` grown the three transfer columns as parameters, and `account_balance_snapshots.calculated_balance_minor_units` + a generated `difference_minor_units` + `adjustment_transaction_id` (so a reconciliation's calculated-vs-confirmed difference and its resulting adjustment transaction are both queryable facts, not values computed once and discarded) — and (PROMPT 14) `recurring_rules.status` (`active`/`paused`/`ended`, replacing the old bare `is_active` boolean), `auto_create_mode`/`reminder_lead_days`, and `related_person_id`; `recurring_rules.next_due_date` became nullable (null once a rule with an `end_date` has nothing left to schedule); two new tables — `recurring_rule_amount_schedules` (a scheduled future amount change, so "amount change from a particular date" is a real, dated row rather than an in-place edit) and the append-only `recurring_rule_events` (the full "recurrence history" audit log: created/amount_scheduled/paused/resumed/skipped/ended/occurrence_generated, never updated or deleted) — plus a partial unique index `transactions_recurring_rule_occurrence_uidx` on `(recurring_rule_id, transaction_date)` that makes a duplicate occurrence for the same rule and date impossible at the database layer, and three new SECURITY INVOKER RPCs (`set_recurring_rule_status`, `skip_recurring_rule_occurrence`, `record_recurring_rule_occurrence`) that each atomically pair a `recurring_rules` state change with its `recurring_rule_events` log entry (and, for the last one, the generated transaction itself) — every "grown parameters" RPC change in this table is a drop-and-recreate, never `CREATE OR REPLACE`, since Postgres treats an appended parameter as a new overload rather than a true replacement — see `supabase/migrations/`, [database-plan.md](./database-plan.md), and [financial-domain-model.md](./financial-domain-model.md) for the ER diagram. `pnpm db:reset` and `pnpm db:types` both verified working — see [local-supabase.md](./local-supabase.md) |
| Authentication | Implemented and verified end to end against the local Supabase stack: sign up, sign in, sign out, email-verification state (+ resend), forgot/reset password (real emailed link via local Mailpit), onboarding, protected routes, safe post-auth redirects, expired/invalid-link handling. See `src/features/auth/`, `src/lib/auth/`, `src/lib/supabase/middleware.ts`, `src/app/auth/callback/route.ts` |
| Household / tenant boundary | Implemented and verified: onboarding creates a household (name, base currency, timezone, financial month start day) and owner membership atomically and idempotently (a retry never creates a duplicate — see `household_memberships_one_owner_per_user`); centralized authorization helpers in `src/lib/households/permissions.ts`; two independent users' households/memberships/financial records proven mutually inaccessible via real access tokens in `tests/e2e/household-isolation.spec.ts`. Collaboration invitations (the `invited` status) are schema-ready but have no flow built yet — see PROMPT 4 |
| Design components | No visual design delivered by Claude Design yet. Placeholder shadcn/ui (Radix-based) primitives in `src/components/ui/`, plus shared layout/state components in `src/components/layout/` and `src/components/shared/` — reconcile against real design tokens when delivered (see [architecture.md](./architecture.md) §4) |
| App shell / privacy mode | Implemented and verified: responsive sidebar + mobile sheet nav (shared nav config in `src/lib/navigation.ts`, route-aware `aria-current`), header (household selector foundation, command-search dialog, hide-balance toggle, user menu with sign out), all gated in `src/app/(workspace)/app/layout.tsx` so no nested route can render the shell without a session/profile/household. Privacy mode (`src/components/shared/privacy-provider.tsx`) is a cookie-backed preference read server-side on every request — SSR and the client's first render always agree, so toggling it survives a refresh with no flash of revealed amounts and no hydration mismatch; concealment is display-only (`SensitiveAmount`), never touches stored values, and no amount is ever put in a page title/metadata. 20 launched sections have routes (5 are stubs — see [product-scope.md](./product-scope.md)); unfinished advanced modules are not linked in navigation at all |
| Routes/pages | `/` (landing), `/login`, `/signup`, `/forgot-password`, `/reset-password`, `/auth/callback` (route handler), `/onboarding` (auth-gated, also creates the household), `/app` + `/app/{people,institutions,cash-flow,accounts,income,expenses,transfers,recurring,investments,calculators,debts,lending,liabilities,insurance,assets,money-drains,goals,emergency-fund,net-worth,monthly-closing,documents,reports,settings}` plus `/app/accounts/[accountId]`, `/app/cash-flow/categories`, `/app/income/[incomeSourceId]`, `/app/recurring/[ruleId]`, `/app/investments/portfolio`, `/app/investments/staking`, `/app/debts/[loanId]`, `/app/debts/dashboard`, `/app/lending/[lendingId]`, `/app/liabilities/[liabilityId]`, `/app/insurance/[policyId]`, `/app/money-drains/[moneyDrainId]`, `/app/goals/[goalId]`, and `/app/monthly-closing/[closingId]` (auth + onboarding + household-gated; `/app`, `/app/people`, `/app/institutions`, `/app/accounts` (+ detail), `/app/cash-flow` (+ categories), `/app/income` (+ detail), `/app/expenses`, `/app/transfers`, `/app/recurring` (+ detail), `/app/investments` (+ `/portfolio`, `/staking`), `/app/calculators`, `/app/debts` (+ detail, `/dashboard`), `/app/lending` (+ detail), `/app/liabilities` (+ detail), `/app/insurance` (+ detail), `/app/money-drains` (+ detail), `/app/goals` (+ detail), `/app/emergency-fund`, `/app/net-worth`, and `/app/monthly-closing` (+ detail) have real content, the rest are stubs) |
| Shared data-access layer | Implemented — see [data-access-patterns.md](./data-access-patterns.md): `src/lib/mutations` (`runHouseholdMutation`, the standard 8-step mutation pipeline: user + household authorization → validated input → caller's write → activity event → revalidation → typed `ActionResult`, never a raw database error); `src/lib/activity` (`recordActivityEvent`); `src/lib/queries/pagination.ts` (pagination, deterministic ordering, household scoping, archived-record filtering); `src/lib/money` (now also `parseDecimalToMinorUnits`, `formatPercentage`, `calculateRatio`, `allocateMoney`); `src/lib/dates` (now also `toMonthKey`, `resolveTimeZone`, `createStatementPeriod`, semantic date aliases); matching branded schemas in `src/lib/validation/primitives.ts`. Now wired up end to end by the People, Institutions, Accounts, Categories, Transactions, Income, Expenses, Transfers, and Recurring commitments modules (see below) |
| People (`/app/people`) | Implemented and verified: create/edit/archive/restore (`src/features/people/actions.ts`, all via `runHouseholdMutation`), search by name, filter by relationship type, explicit "show archived" toggle, pagination — all URL-searchParam-driven so the Server Component page (`src/app/(workspace)/app/people/page.tsx`) stays the source of truth. `relationship_type` includes `self`, so a person can represent the signed-in user or a family member (also lenders/borrowers/nominees/co-owners). Privacy-safe list view: birth date and notes are never shown in the table, only in the edit dialog. "Ownership connections"/"nominee connections" are inert placeholders in the edit dialog pending the Insurance module — Accounts now populates the owner side of that connection. Cross-household access verified in `tests/e2e/people-institutions.spec.ts` |
| Institutions (`/app/institutions`) | Implemented and verified: create/edit/archive/restore (`src/features/institutions/actions.ts`), search by name, filter by type, linked-account count (a single embedded-count Supabase query, no N+1 — see `src/features/institutions/queries.ts`), and a duplicate-warning step (`src/features/institutions/duplicate-detection.ts`: normalized name / website domain / support-phone matching) that blocks the write and shows a warning rather than auto-merging — the user must explicitly resubmit with `confirmDuplicate: true` to proceed. An exact case-insensitive name duplicate is still hard-blocked at the DB layer (`institutions_household_name_idx`). Verified end to end in the browser and in `tests/e2e/people-institutions.spec.ts` (cross-household isolation, enum/format rejection, exact-duplicate rejection, and that a `financial_accounts` row can already reference an institution) |
| Accounts (`/app/accounts`, `/app/accounts/[accountId]`) | Implemented (PROMPT 9, reconciliation grown in PROMPT 13): create/edit/close/reopen (`src/features/accounts/actions.ts`), search by name, filter by type, "show closed" toggle, pagination on the list; a detail page with recent transactions, this month's inflow/outflow, and full balance-snapshot history. **Balance strategy** (documented in `src/lib/calculations/account-balance.ts`, unit-tested): current balance = latest confirmed `account_balance_snapshots` row (or the account's opening balance if none exists) plus every ledger transaction strictly after that baseline date, restricted to `status in (pending, cleared, reconciled)` — `planned` hasn't occurred yet and `cancelled` never did — computed for a whole list page in a fixed number of queries, never one per row; a transfer's fee (if any) reduces only the source side, and a cross-currency transfer credits the destination with its own explicit converted amount rather than the source-side figure (see the Transfers row below). **Reconciliation is now a fully explicit, auditable record** (PROMPT 13): `recordBalanceCorrectionAction` calls `record_account_balance_correction()` (`supabase/migrations/20260721130000_transfers_reconciliation.sql`), which atomically writes one `account_balance_snapshots` row carrying *both* the user-confirmed balance (`balance_minor_units`) and the ledger-calculated balance at that moment (`calculated_balance_minor_units`) — their generated `difference_minor_units` column can never drift from the two source values — plus, only when they differ, a `kind = 'adjustment'` transaction, linked back via the snapshot's `adjustment_transaction_id`. The Balance history table on the account detail page renders Confirmed/Calculated/Difference side by side, so a reconciliation's difference is a visible, permanent fact rather than something computed once and discarded — never a silent rewrite of a past transaction. Closing an account never deletes it (`is_active = false` + `closed_date`, history preserved); institution/owner pickers reuse the Institutions/People list queries. This month's inflow/outflow is `status = 'cleared'` only, per PROMPT 10's "reports use cleared transactions by default" |
| Categories (`/app/cash-flow/categories`) | Implemented (PROMPT 10): the default taxonomy (Income / Essential expenses / Lifestyle / Wealth building, each with PROMPT 10's specified subcategories) is auto-seeded per household by `seed_default_transaction_categories()` — see migration above. Custom categories, parent/subcategory nesting, archive/restore, manual reorder (`reorderCategoriesAction`, up/down within siblings, persisted to `sort_order`), and a need/want/obligation/protection/investment `classification` tag are all implemented in `src/features/transaction-categories/`. System-default categories (`is_system_default`) can be archived like any other but the UI never offers a hard delete |
| Transactions / Cash Flow (`/app/cash-flow`) | Implemented (PROMPT 10): add/edit/archive (→ `status = 'cancelled'`, never deleted)/refund/search/filter (kind, status, account, "show cancelled")/pagination — `src/features/transactions/`. **Splits**: `create_transaction_with_splits()`/`update_transaction_with_splits()` (SECURITY INVOKER RPCs, see migration above) write a transaction and its `transaction_splits` atomically, and the Server Action pre-validates the split amounts sum to the transaction total before calling the RPC at all, so a mismatch never becomes a raw Postgres error. **Refunds**: `recordRefundAction` always creates a new `kind = 'refund'` row with `reverses_transaction_id` set — never edits the original expense — and is capped at the original amount minus whatever's already been refunded against it (computed via a batched, no-N+1 query, same pattern as `src/features/accounts/queries.ts`). **Transfers** are structurally excluded from `cash_flow_transactions` (unchanged from the core-ledger migration) and the transaction form itself refuses to submit a transfer without two distinct accounts. **Investment/loan/lending kinds** (`investment_contribution`, `loan_disbursement`, etc.) are offered in the general form's kind picker so those amounts are never miscategorized as `expense`; `refund` and `adjustment` are deliberately not offered there (they have their own dedicated flows). Account relation, person relation, source-type flag (manual/import — recurring/system reserved for future automation), and the five-state status lifecycle are all implemented; a receipt-attachment control is not (the `attachments` table already supports `attachable_type = 'transaction'`, but no upload UI exists until the Documents module) |
| Income (`/app/income`, `/app/income/[incomeSourceId]`) | Implemented (PROMPT 11): `income_sources` CRUD (create/edit/archive/restore — `src/features/income/actions.ts`) plus `recordIncomeAction`, the only path that writes a transaction (**source setup never itself creates income** — the acceptance criterion is enforced by construction: the create/update actions only ever touch `income_sources`). **Schedule math** (`src/lib/calculations/income-schedule.ts`, pure and unit-tested): `computeIncomeSchedule` derives each occurrence directly from its index (never by repeatedly stepping from the previous one), so a clamped short month never drags every later occurrence earlier; `isIncomeMissed` flags a source once its most-recently-due occurrence's grace period (default 7 days) has elapsed with nothing received on or after it. **Duplicate detection**: `recordIncomeAction` checks for another non-cancelled receipt against the same source within a ±10-day window and returns a `duplicate_warning` outcome (same explicit-confirm pattern as Institutions' duplicate flow, PROMPT 8) rather than silently blocking or silently double-recording. **Income by source / by person / trend**: `getIncomeBySource`/`getIncomeByPerson`/`getIncomeTrend` in `src/features/income/queries.ts` all read `kind = 'income'` and `status = 'cleared'` only, so monthly totals match the "reports use cleared transactions by default" rule from PROMPT 10 and are never inflated by a planned/pending/cancelled entry. Institution/employer/client is modeled as an existing `institutions` row (no separate concept), and "person earning" is a `people` row, both nullable |
| Expenses (`/app/expenses`) | Implemented (PROMPT 12): a dedicated analysis-and-views layer over the same `transactions`/`kind = 'expense'` rows Cash Flow already manages — `src/features/expenses/` reuses `archiveTransactionAction`/`restoreTransactionAction`/`recordRefundAction`/`getTransactionSplitsAction` directly from the Transactions feature rather than duplicating them (a "use server" file can't re-export another module's Server Actions, so each call site imports them directly). **Views**: all / this month / essentials / discretionary / irregular (one-time) / recurring / unplanned all narrow the same paginated list (`listExpenses`); by-family-member / by-merchant / by-category render a grouped breakdown table instead, each row linking back into the filtered flat list. **Required fields**: amount, date, account, category (or a split across categories), merchant/counterparty, person, notes, and receipt reuse the existing `transactions` columns and the PROMPT 9 `attachments` table (now with a working upload path — see the migration note above); **planned vs unplanned** is the new `transactions.is_planned` column; **one-time vs recurring** is `recurring_rule_id`'s presence — marking an expense recurring for the first time creates a minimal `recurring_rules` row (`createRecurringRuleForExpense`), and un-marking one deactivates it, but editing an already-recurring rule's cadence is out of scope (no Recurring Rules management feature exists yet). **Analysis** (`src/lib/calculations/expense-analysis.ts`, pure and unit-tested, plus `src/features/expenses/queries.ts`): category totals, essential/discretionary amounts (`bucketClassification` — need/obligation/protection → essential, want → discretionary), unplanned amount, average daily spend, and month-over-month comparison are all computed **net of refunds** (`netExpenseAmount`), attributing a refund back to the original expense's own category/person/month regardless of which period the refund itself landed in. Largest expenses ranks by net amount over a bounded overfetch. Expense reads always filter `kind = 'expense'` directly (never the `cash_flow_transactions` view, which — like any `select *` view — froze its column list at creation time and doesn't expose columns added to `transactions` afterward, including `is_planned`), so transfers/investment/loan rows can never leak into a total. Verified end to end against the local Supabase stack: create/refund/exclude-transfers-and-investments all confirmed working in the running app |
| Transfers (`/app/transfers`) | Implemented (PROMPT 13): one dedicated form (`TransferDialog`) for creating/editing a transfer — amount, date, source/destination account, optional fee, status, notes, and (only when the two selected accounts' currencies actually differ, per a client-side lookup that's purely informational) an explicit converted amount + exchange rate. `createTransferAction`/`updateTransferAction` re-derive both accounts' real currencies server-side (never trusting a client-supplied currency code for this branch) and enforce PROMPT 13's rule directly: a cross-currency transfer without both fields throws before the RPC is ever called; a same-currency transfer with either field set has them nulled out. **Balanced effects**: a transfer is one `transactions` row read from both accounts (see `signedContribution` in `src/lib/calculations/account-balance.ts`) — the fee subtracts from the source side only, and the destination side credits `transfer_destination_amount_minor_units` when set (cross-currency) or the same source-side amount otherwise (same-currency), so archiving/restoring a transfer removes or restores its effect on *both* balances at once by construction, not by two separate writes. **Reversal**: `reverseTransferAction` creates a new transfer with the source/destination accounts swapped, linked back via `reverses_transaction_id` (extended — see the migration note above — to also allow a transfer reversing a transfer, alongside the pre-existing refund-reverses-expense rule) — same-currency only, since reversing a cross-currency transfer would need a fresh, separately-entered exchange rate for the reverse direction, and this app never invents one; a transfer that's already been reversed, or that itself reverses currencies, is rejected with a clear message rather than silently reused. Archiving reuses `archiveTransactionAction`/`restoreTransactionAction` directly from the Transactions feature. Verified end to end against the local Supabase stack: same-currency transfer with a fee, cross-currency transfer with an explicit rate (both rejected without the required fields and accepted with them), reversal, and archive — all confirmed to move the right amount on both accounts' calculated balances in the running app |
| Recurring commitments (`/app/recurring`, `/app/recurring/[ruleId]`) | Implemented (PROMPT 14): `recurring_rules` finally has a working feature (schema-only since PROMPT 9) — create/edit its template (name, kind, account/category/counterparty/person, frequency, interval, start/end date, auto-create mode, reminder lead days) via `src/features/recurring/`. **Schedule math** (`src/lib/calculations/recurring-schedule.ts`, pure and unit-tested, generalizing PROMPT 11's income-schedule.ts pattern with an `intervalCount` multiplier and a day-based `custom` cadence): every occurrence is computed directly from its index against the rule's own `start_date`, never by repeatedly stepping from the previous occurrence, so a clamped short month never drags every later occurrence earlier — verified live with a monthly rule anchored to the 31st. `next_due_date` is a single, explicitly-advanced pointer (never recomputed by scanning from scratch), moved forward only by `skip_recurring_rule_occurrence`/`record_recurring_rule_occurrence`, so dates are plain calendar values with no time-of-day component to drift across timezones. **No duplicate occurrence is generated**: enforced structurally by a partial unique index on `(recurring_rule_id, transaction_date)`, not just application discipline. **Auto-create behavior**: `generateDueOccurrencesAction` (triggered by a "Generate due occurrences" button — no scheduled-job infrastructure exists in this app yet, so this is also what a future cron would call) creates a `status = 'planned'` transaction for every elapsed occurrence of every active `auto_create_mode = 'planned_transaction'` rule, **never `'cleared'`** — PROMPT 14: "prefer generating planned occurrences ... rather than silently marking money as paid." `reminder_only` rules instead surface as upcoming/missed for a human to confirm via "Record occurrence." **Pause/resume**: `next_due_date` is frozen while paused and never fast-forwarded on resume — verified live that all past occurrences remain exactly as they were. **Skip one occurrence**: logs the skip and advances `next_due_date` past it without ever writing a transaction — verified live. **Amount change from a particular date**: `scheduleAmountChangeAction` always inserts a new, dated `recurring_rule_amount_schedules` row (rejecting an effective date in the past) rather than editing the rule's own amount in place; `resolveAmountForDate` (unit-tested) resolves the amount for a given occurrence date from that schedule, so a change scheduled for the future can never retroactively touch an occurrence already generated as its own independent transaction row — verified live: scheduling ₹100→₹150 from a future date left three already-generated occurrences at ₹100 and the rule's "current amount" unchanged until that date arrives. **Recurrence history**: the append-only `recurring_rule_events` log (created/amount_scheduled/paused/resumed/skipped/ended/occurrence_generated) renders as a timeline on the rule's detail page, alongside its generated occurrences and scheduled amount changes. Upcoming/missed commitments are computed from `next_due_date` plus a grace period, same convention as PROMPT 11's missed-income indicator |
| Dashboard (`/app`) | Implemented (PROMPT 15): the first real cash-flow dashboard, replacing the all-placeholder cards from the foundation phase. `src/lib/calculations/cash-flow-summary.ts` (pure, unit-tested) documents exactly which transaction kinds feed each figure and defines free cash flow (income − expenses − debt payments), savings rate (free cash flow ÷ income), and investment rate (investments ÷ income) — transfers, opening balances, valuation gains, loan disbursements, and investment withdrawals are never counted, by construction. `src/features/dashboard/queries.ts` computes available account balance (open accounts in the household's base currency only — no invented FX conversion), the month-to-date cash-flow summary (income/expenses/investments/debt payments/insurance premiums, reusing `getExpenseSummary`/`getExpenseByCategory` from the Expenses feature rather than re-deriving refund-netting), a 6-month cash-flow trend, and a 6-month account-balance trend (reusing `computeAccountBalance` per month-end cutoff, same balance strategy as PROMPT 9). Every KPI card links to its underlying records (`SummaryCard` grew an optional `href`; the Cash Flow page grew `dateFrom`/`dateTo` deep-link support). Five Recharts charts (`src/features/dashboard/charts/`) — income vs. expenses, expenses by category, a cash-flow breakdown (this dashboard's flow-order equivalent of a waterfall), account-balance trend, and monthly savings trend — each wrapped in a shared `ChartCard` (`src/components/charts/chart-card.tsx`) providing an accessible text summary, an empty state, a date range + data-cutoff caption, and privacy-mode concealment that (like `SensitiveAmount`) never places a real figure in the render output while concealed, verified end to end in the running app together with real transaction data flowing through every card and chart correctly |
| Investments — schema (`supabase/migrations/20260722100000`–`20260722100005`) | Implemented (PROMPT 16): six tables, deliberately kept distinct so no investment is ever "a single mutable value" — `investment_accounts` (platform/custody account, no `account_type` enum since one account holds many asset classes), `investment_assets` (the specific security/instrument, household-scoped, 17-value `asset_class` check constraint matching PROMPT 16's list exactly), `investment_holdings` (links one account to one asset, `unique (investment_account_id, investment_asset_id)` — "multiple holdings can exist on one platform" is structural, not a convention; carries no quantity/value column at all), `investment_transactions` (kind-discriminated by `transaction_type` — contribution/purchase/sale/dividend/interest/fee/withdrawal — `quantity`/`price_per_unit` as paired-nullable `numeric`, not minor units, same precision reasoning as `transactions.exchange_rate`; a nullable `linked_transaction_id` optionally bridges to the core ledger so PROMPT 15's dashboard keeps working off one source of truth; editable + soft-cancelable like the core ledger, not append-only), `investment_valuation_snapshots` (append-only — verified no `UPDATE`/`DELETE` grant for `authenticated` — `source` is exactly `manual`/`imported`/`institution_statement`/`calculated` per the prompt), and `investment_documents` (its own table per the prompt's explicit list, three plain nullable FKs to account/holding/valuation-snapshot with an at-least-one `CHECK`, rather than extending `attachments`' polymorphism). A household-consistency trigger on every table also enforces that `investment_transactions`/`investment_valuation_snapshots`' `currency_code` matches their holding's asset currency — a holding can never silently combine currencies (PROMPT 16 acceptance criterion; see docs/product-scope.md §4, no FX conversion engine in v1). Verified structurally against the local Supabase stack: multiple holdings per account, contribution totals staying distinct from the latest valuation, historical valuations queryable across dates, and currency-mismatch/duplicate-holding/target-less-document all correctly rejected |
| SIPs (`/app/investments`) | Implemented (PROMPT 17): `investment_sips` (references one `investment_holdings` row rather than duplicating asset/platform as separate columns — see PROMPT 16), append-only `investment_sip_events`, and `investment_transactions.investment_sip_id` + a partial unique index on `(investment_sip_id, transaction_date)` — the hard database-layer backstop against ever double-counting a contribution ("avoid double counting" acceptance criterion, verified live: a duplicate-date contribution is rejected outright). `record_investment_sip_contribution()` (SECURITY INVOKER RPC) atomically writes all four required effects per PROMPT 17 — the core-ledger transaction (`kind = investment_contribution`, so the contribution account's calculated balance and PROMPT 15's dashboard both pick it up), the linked `investment_transactions` contribution row, the schedule advance, and the event log — verified live to affect the cash account exactly once. Daily and monthly (and every other) cadence reuses `src/lib/calculations/recurring-schedule.ts` directly rather than re-deriving schedule math; `src/lib/calculations/sip-commitment.ts` normalizes any cadence into monthly/daily-equivalent figures for the "total monthly commitment"/"daily commitment" views, verified against both PROMPT 17 example scenarios (₹20/day, ₹100/month) with unit tests. A missed SIP is never treated as a completed contribution — the "missed" view reads only `next_due_date`/`status`, never `investment_transactions`. `src/features/investment-sips/` has full CRUD + lifecycle UI (planned/active/paused/completed/cancelled, with automatic completion on schedule exhaustion) and all 9 requested views (active/upcoming/completed-this-month/missed/paused/total-monthly-commitment/daily-commitment/platform-distribution/asset-class-distribution). No dedicated management UI exists yet for `investment_accounts`/`investment_assets` (PROMPT 16 was schema-only), so the SIP dialog resolves a "+ create new" platform/asset inline — the same pattern PROMPT 12's `createRecurringRuleForExpense` established for a supporting entity with no management screen yet |
| Portfolio & performance (`/app/investments/portfolio`) | Implemented (PROMPT 18): `src/lib/calculations/portfolio-performance.ts` (pure, unit-tested) computes every core metric via **weighted-average cost basis** (this app has no per-lot tracking) — total contributed, current value, realized/unrealized gain-loss, income received, fees, and absolute return — plus descriptive (non-advice) liquidity/risk classifications defaulted per asset class. `src/lib/calculations/xirr.ts` is the annualized-return engine: the formula and numerical method (Newton-Raphson with a bisection fallback) are documented in the module itself, every non-convergence case (too few cash flows, no sign change, same-date flows, no bracket in [-99%, +1000%], iteration exhaustion) returns a typed reason with a human-readable message rather than a wrong number or a crash, and 14 tests cover analytically-verified rates, self-consistency (NPV at the returned rate ≈ 0) for realistic multi-flow SIP-like series, and every non-convergence path. **Valuation gain is never treated as income**: `incomeReceivedMinorUnits` is built only from `dividend`/`interest` transaction rows; unrealized gain/loss is computed separately from the latest valuation snapshot and never touches the income figure. **Principal and growth are visually separable**: the "principal vs. growth" chart renders them as two distinct stacked-bar series (`src/features/investments/charts/principal-vs-growth-chart.tsx`), reconciling exactly against the "portfolio value over time" chart. **Missing/stale valuations are always shown, never silently zeroed**: a holding with no valuation yet gets `hasValuation: false` (excluded from totals, not counted as ₹0) and a "No valuation" badge; one older than 45 days reuses `StaleDataIndicator`. All 6 requested charts plus a holdings table are wired into one page; asset/platform allocation and gain/loss-by-holding are scoped to one currency at a time, never blended |
| Staking & daily value tracking (`/app/investments/staking`) | Implemented (PROMPT 19): `staking_positions` (references one `investment_holdings` row, same derive-not-duplicate pattern as SIPs) and revision-versioned append-only `staking_daily_snapshots` — `unique (staking_position_id, snapshot_date, revision)` plus a CHECK requiring a non-empty `adjustment_reason` whenever `revision > 1` is the database-layer enforcement of "one snapshot per position per day unless adjustments are explicitly versioned"; a `staking_daily_snapshots_current` view (`DISTINCT ON (position, date) ORDER BY revision DESC`) surfaces the latest revision everywhere the app reads snapshot data. Every row's `closing_value_minor_units` is CHECK-constrained to equal `opening + contribution + reward - withdrawal - fee` (`reward_minor_units` may be negative — a slashing/penalty event); `src/lib/calculations/staking-snapshot.ts` (pure, unit-tested) validates the same equation client-side before any write is attempted, so a mismatch is a friendly message, never a raw constraint violation, and `recordDailySnapshotAction` derives `reward` server-side from the other four fields rather than collecting it directly, so the equation can never be entered out of balance from the UI. **Expected projection is never shown as a guarantee** (PROMPT 19 acceptance criterion): `computeExpectedProjection` uses closed-form daily compounding (`principal × (1 + rate)ⁿ`, computed independently per day, not an iterative running product, so rounding at one day never drifts into the next), `validateExpectedDailyRate` rejects impossible rates (≤ -100%, or > +50%/day — the latter catching the common "typed 5 meaning 5%" mistake) and warns on merely-suspicious ones (> 5%/day), and every chart touching the projection (`expected-projection-chart.tsx`, `actual-vs-expected-chart.tsx`) renders it dashed with an explicit "assumption, not a guarantee" caption, in a field/column entirely separate from actual closing values. **Risk treatment** (`src/features/staking/queries.ts`): high-return warning (unusually high `expected_daily_rate`), platform concentration (share of latest value per `investment_accounts` platform), snapshot staleness (`SNAPSHOT_STALE_AFTER_HOURS = 48` — much shorter than the 45-day threshold PROMPT 18 uses for periodic investment valuations, matching a daily-tracked position's cadence), and a manually-entered-vs-other-source ratio are all read layers over the two tables, not new schema. All 6 requested charts (actual closing value, actual vs. expected, total contributed, accumulated rewards, withdrawals, expected projection) plus a positions table are wired into one page, cross-linked with the SIPs and Portfolio pages. Verified end to end against the local Supabase stack, including through the real browser UI (not just direct SQL): the "record snapshot" dialog auto-detects an existing date, requires and rejects a missing adjustment reason, and a versioned adjustment (revision 2 then 3) correctly updates the position's latest value and cumulative reward chart while leaving every prior revision permanently in place |
| Financial calculators (`/app/calculators`) | Implemented (PROMPT 20): six pure, unit-tested calculators in `src/lib/calculations/calculators/` — SIP projection (geometric per-period rate conversion plus an annual step-up, never a naive `rate/periodsPerYear`), lump sum (`FV = P(1+r/n)^(nt)`), daily growth (day-by-day compounding with an optional recurring contribution/withdrawal schedule), EMI (`payment = Pr(1+r)^n/((1+r)^n-1)` plus a full amortization schedule), loan prepayment (derives remaining tenure from principal/rate/EMI, then compares an untouched schedule against one with a lump-sum prepayment applied — never throws; a non-amortizing EMI returns a typed failure, same convention as `xirr.ts`'s non-convergence reasons), and goal funding (inflates a today's-value target forward to the target date, then solves for the required monthly contribution). `src/lib/calculations/calculators/rate-validation.ts` bounds-checks every annual-rate input (reused daily-rate bounds come from `staking-snapshot.ts`'s `validateExpectedDailyRate` directly, so a calculator's daily rate can never accidentally reuse or drift from an annual-rate bound). Every calculator runs entirely client-side — recomputed on each keystroke via pure functions, no server round trip to compute a projection — except explicitly saving or deleting a scenario (`src/features/calculators/actions.ts`, via the new household-scoped `calculator_scenarios` table: `calculator_type`, `name`, `inputs`/`outputs` jsonb frozen at save time, optional `linked_account_id`). **Account-linked calculators**: lump sum, daily growth, and goal funding can prefill their principal/starting/current-amount field from a real account's calculated balance (`AccountLinkField`), editable afterward — EMI, loan prepayment, and SIP projection are standalone-only since their inputs don't correspond to a trackable account balance. Every result is labeled a projection (a `Badge`/caption, never guaranteed), every methodology is disclosed inline (`MethodologyNote`), and principal/growth (or principal/interest) are charted as visually distinct stacked-bar series reusing the `ChartCard` pattern from PROMPT 15/18. Verified end to end in the running app: all six tabs compute correctly, the account-link prefill works, and save/delete-scenario both round-trip through the database |
| Loans & debt (`/app/debts`, `/app/debts/[loanId]`) | Implemented (PROMPT 21): `loans` (10 types — education/personal/business/home/vehicle/gold/credit_card/family/informal/other; lender is an institution or a person, `loans_requires_a_lender` CHECK enforcing at least one — the first "at least one of X/Y" constraint in this schema, since `income_sources`/`financial_accounts`' analogous institution-or-person pairs left it unenforced; education-loan fields — course, study period, moratorium + required end date, interest subsidy — nullable and usable regardless of `loan_type` at the schema layer, UI-gated) and append-only `loan_payments` (principal/interest/fee/penalty components, `total_payment_minor_units` CHECK-equal to their sum, `overpayment_amount_minor_units`, a `reverses_payment_id` self-reference for corrections — same shape as `transactions.reverses_transaction_id`, not staking's revision-versioning, since a discrete payment event doesn't need "one value per day" semantics). **Outstanding principal is always derived, never stored** (`src/lib/calculations/loan-outstanding.ts`, pure and unit-tested): `disbursedAmount − totalPrincipalPaid`, floored at zero, with `selectEffectivePayments` excluding a reversed payment and its reversal from every computation. A principal component that would push outstanding below zero is never silently accepted or silently rejected — `recordLoanPaymentAction` computes the actual overpayment amount server-side and returns a typed `overpayment_warning` outcome (same shape as PROMPT 11's `duplicate_warning`) unless the caller explicitly resubmits with `confirmOverpayment: true`, satisfying PROMPT 21's "cannot become negative without explicit overpayment handling" by construction. `record_loan_disbursement()`/`record_loan_payment()` (SECURITY INVOKER RPCs, mirroring PROMPT 17's `record_investment_sip_contribution` pattern) atomically write the core-ledger transaction (`kind = loan_disbursement`/`loan_payment` — both already reserved on `transactions.kind` since PROMPT 10, so disbursed money was structurally never `income` to begin with) alongside the `loans`/`loan_payments` row; `record_loan_payment` also auto-closes the loan once outstanding reaches zero, and `reverseLoanPaymentAction` reopens a `closed` loan back to `active` if a reversal brings outstanding back above zero (a bug caught and fixed during live browser verification — a loan closed by auto-close must not stay "Closed" once the payment that closed it is reversed). Reversing a payment record never touches its linked transaction — a genuinely wrong cash movement is corrected separately from Cash Flow. Two dashboard figures that already existed but had nothing to read (`debtPayment`/`getKindTrend("loan_payment")` in PROMPT 15's `src/features/dashboard/queries.ts`) now populate for the first time. Full CRUD (`src/features/loans/`) plus disburse/record-payment/reverse-payment/status dialogs and two debt charts (outstanding balance over actual payments, payment breakdown by component) satisfy "debt charts use actual payments and balances." Verified end to end against the local Supabase stack, including through the real browser UI: create with lender-institution + borrower-person, disburse (confirmed excluded from the dashboard's "Income this month" figure while correctly raising the account balance), record a split EMI payment (outstanding updates, linked transaction and activity event both confirmed), a deliberate overpayment (warned, then confirmed, outstanding floored at zero, loan auto-closed, excess tracked separately), and a reversal (loan correctly reopened to active) |
| Lending & receivables (`/app/lending`, `/app/lending/[lendingId]`) | Implemented (PROMPT 23): `lendings` (borrower is a person or an institution/company via an "at least one" CHECK, same shape as loans' lender — PROMPT 21) and append-only `lending_repayments` (`principal_component_minor_units` + `interest_component_minor_units`, summing to `total_repayment_minor_units`; a correction is a new row referencing the original via `reverses_repayment_id`, never an edit — same reversal pattern as `loan_payments`). **Unlike a loan, there is no pre-disbursement stage** — PROMPT 23's own status list (active/partially_repaid/repaid/delayed/disputed/written_off) has nothing before "active," so `create_lending()` (SECURITY INVOKER RPC) writes the lending row and its one-time disbursement transaction (`kind = lending_disbursement` — already reserved on `transactions.kind` since PROMPT 10, so money lent was structurally never a consumption expense to begin with) atomically at creation, rather than loans' separate create-then-disburse two-step. `record_lending_repayment()` mirrors `record_loan_payment`: writes the linked transaction (`kind = lending_repayment` — never `income`, satisfying "principal repayment is not income" by construction) and the repayment row together, then auto-advances status toward `repaid` as principal is recovered (`active`/`delayed` → `partially_repaid` → `repaid`) — `disputed`/`written_off` are always a deliberate, manual status change (`setLendingStatusAction`, wired to a "Change status" menu on the detail page). **Outstanding is always derived, never stored** (`src/lib/calculations/lending-outstanding.ts`, pure and unit-tested, mirroring `loan-outstanding.ts`): `amountLent − totalPrincipalRecovered`, floored at zero; a principal component that would push it negative surfaces an inline "excess repayment" confirmation instead of a silent negative balance or a hard rejection (same `overpayment_warning` shape as loans). **A written-off lending is never deleted or hidden** (PROMPT 23 acceptance criterion): its record and outstanding figure remain fully visible on its own detail page and in the repayment history table — `written_off` is simply excluded from "current outstanding" totals (`src/lib/calculations/lending-metrics.ts`'s `computeLendingTotals`), the same way a `repaid` loan isn't counted as active debt. **Views**: total lent (lifetime, every status), total outstanding (currently-owed statuses only), principal recovered/interest received (tracked as separate lifetime totals, so interest can be reported independently of principal — the other PROMPT 23 acceptance criterion), overdue and upcoming (both computed fresh from the real `expected_repayment_date` and derived outstanding — never a stored flag, same "computed, never stored" rule as PROMPT 22's `computeOverdueAmount`), borrower exposure (a horizontal-bar chart grouped by borrower, modeled on the debt dashboard's lender-exposure chart), and recovery history (a line chart of outstanding after every actual repayment — built only from real `lending_repayments` rows, never a projection) — all on one `/app/lending` overview page alongside the manage list, rather than split into a separate dashboard route the way PROMPT 22 split from PROMPT 21, since PROMPT 23 asked for both in one prompt. "Documents" is schema-ready (`attachments.attachable_type` grown to include `'lending'`) but has no upload UI yet, the same gap every attachable type beyond financial_account/transaction already has. `pnpm db:reset` applies the migration cleanly, generated types round-trip, and typecheck/lint/unit tests/build all pass with the new routes registered. Verified end to end in the running app: create a lending (writes the disbursement transaction, appears in every overview card/chart), a partial repayment (outstanding/status/recovery-history chart all update, principal vs. interest correctly separated), a deliberate excess repayment (warned with the exact excess figure, then confirmed), a reversal (correctly nets the excess back out, status reverts), and marking `written_off` (immediately excluded from "total outstanding" and from the borrower-exposure chart while its own record and outstanding figure stay fully visible on the detail page, the manage list, and a dedicated "Written off" banner) — zero console errors throughout |
| Informal borrowing & liabilities (`/app/liabilities`, `/app/liabilities/[liabilityId]`) | Implemented (PROMPT 24): one table, `liabilities`, covers two related-but-distinct real-world concepts via `liability_source` — **informal borrowing** (money borrowed from family/a friend, an employer advance, an unpaid obligation, private business borrowing, a pending personal settlement) and **general obligation** (unpaid tax, pending bill, contractual commitment, guarantee, maintenance obligation, recurring draining commitment) — plus append-only `liability_payments` (same shape as `lending_repayments`). Unlike loans/lendings' "at least one" lender/borrower CHECK, `counterparty_person_id`/`counterparty_institution_id` are both optional here, since a general obligation like an unpaid property tax often has no tracked institution row. **Receiving cash is optional at creation, not mandatory like lending's atomic disbursement**: `create_liability()` (SECURITY INVOKER RPC) only writes the matching `liability_incurred` transaction (never income) when a `receiving_account_id` is actually given — a general obligation, or an "unpaid obligation" for services already consumed with no cash movement, gets no transaction at all rather than a fabricated one. `transactions.kind` grew `liability_incurred`/`liability_payment` (also added to the general Cash Flow form's kind picker and `src/lib/calculations/account-balance.ts`'s `signedContribution`, same treatment as every other loan/lending kind — PROMPT 10's "never miscategorized" rule). `record_liability_payment()` mirrors `record_lending_repayment`: writes the linked transaction (`kind = liability_payment` — never `expense`, since the economic "spend" was already recognized when the liability was incurred, same reasoning as a loan payment) and the payment row together, auto-advancing status toward `paid` as principal is paid down (`active`/`disputed` → `partially_paid` → `paid`); `disputed`/`waived` are always a deliberate, manual status change via a "Change status" menu on the detail page. **Outstanding is always derived, never stored** (`src/lib/calculations/liability-outstanding.ts`, pure and unit-tested, mirroring `lending-outstanding.ts`): `amount − totalPrincipalPaid`, floored at zero; a principal component that would push it negative surfaces an inline "excess payment" confirmation, same shape as loans/lending. **"Do not mix estimates with legally confirmed obligations without labels"** (PROMPT 24 acceptance criterion): every liability has a required `certainty` (`confirmed`/`estimated`) column, always rendered as a visible badge and broken out as its own separate total (`src/lib/calculations/liability-metrics.ts`'s `computeLiabilityTotals`) — never silently folded into a blended figure. **"Integrate with total debt but keep institutional and informal debt distinguishable"**: `computeCombinedDebtBreakdown` combines loans' own `getDebtSummary` (institutional) with this module's informal/general totals into one combined figure that still keeps every source individually visible — four labeled summary cards (combined/institutional/informal/general) plus a by-category breakdown on the `/app/liabilities` overview page. **A waived liability is never deleted or hidden** (mirroring PROMPT 23's written-off rule): its record and outstanding figure remain fully visible on its own detail page and in the manage list, simply excluded from "currently owed" totals. `documentation_status` (none/verbal agreement/written note/formal agreement/legal document) is tracked as its own required field, separate from `certainty`. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/519 unit tests/build all clean) and live in the running app: an informal-borrowing liability with cash received (writes the incurred transaction, appears in every summary card), a general-obligation liability marked `estimated` with no receiving account (no transaction written, correctly flagged in its own "Estimated" banner), a partial payment, a deliberate excess payment (warned with the exact figure, then confirmed), a reversal (correctly nets the excess back out, status reverts), and marking `waived` (immediately excluded from "total debt"/"informal borrowing" while its own record and outstanding figure stay fully visible in the manage list) — zero console errors throughout |
| Insurance (`/app/insurance`, `/app/insurance/[policyId]`) | Implemented (PROMPT 25): `insurance_policies` — 12 types (health/term/life/vehicle/home/personal_accident/travel/business/property/crop/device/other), an Institution insurer, a Person policyholder/nominee, coverage/premium (amount + `premium_frequency` cadence), start/renewal/expiry dates, `status` (active/expired/lapsed/cancelled/renewed), agent/support/claim contacts, a masked policy number, and 16 health-only fields (deductible, co-pay, room-rent restriction, pre-existing conditions declared, waiting periods, network-hospital notes, cashless availability, restoration benefit, no-claim bonus, OPD/consumables cover, pre/post-hospitalization days, day-care treatment, exclusions summary) — nullable and usable regardless of `policy_type` at the schema layer, UI-gated to health, same convention as loans' education fields — plus `insurance_policy_insured_people` (many-to-many, **"multiple people can be insured under one policy"** PROMPT 25 acceptance criterion, always written atomically with the policy by `create_insurance_policy()`). **"Renewal does not overwrite the old policy period"**: renewing calls `create_insurance_policy()` again with `previousPolicyId` set — it always inserts a brand-new row (linked back via `previous_policy_id`) and only flips the *old* row's `status` to `renewed` in the same call; the old row's own dates/coverage/premium/insured-people are never touched by an UPDATE (verified live — see below). **"Premium payment creates a cash transaction"**: unlike loans/lending/liabilities, a premium payment has no outstanding principal to pay down, so it is deliberately a plain `kind = 'expense'` transaction (`transactions.insurance_policy_id`, categorized under the household's existing `classification = 'protection'` category — the same category PROMPT 15's dashboard already reads "insurance premiums" from as a subset of Expenses) rather than a new dedicated transaction kind or a second append-only payments table — `recordPremiumPaymentAction` is a single-table insert, no RPC needed. **"Documents remain private"**: `attachments.attachable_type` grew an `'insurance_policy'` branch — already true structurally (private Storage bucket, household-scoped RLS, signed-URL-only reads), no new upload UI (same gap every attachable type beyond financial_account/transaction has). **"Do not represent entered policy data as a complete legal interpretation"**: every create/edit/renew dialog and the detail page carry a standing disclaimer that this is a tracking summary, never a legal interpretation of the actual policy document. Total annual premium commitment (`src/lib/calculations/insurance.ts`'s `annualizePremiumMinorUnits`, normalizing monthly/quarterly/half-yearly/yearly/one-time onto one axis, mirroring `sip-commitment.ts`) and a purely-advisory "due for renewal soon" list (`isRenewalDueSoon`, computed fresh from the stored renewal/expiry date, never overriding the household's own manually-set `status`) are summarized on the `/app/insurance` overview page. **One real bug caught and fixed during this build**: PostgREST cannot resolve a self-referential embed of `insurance_policies` onto itself via an FK hint (`insurance_policies!insurance_policies_previous_policy_id_fkey(...)`) even though the constraint is correctly named and present — confirmed directly against the REST API (`PGRST200`, even after an explicit schema-cache reload) — fixed by fetching the previous-policy name in a separate batched query instead, the same no-N+1 pattern used everywhere else in this codebase; `transaction_categories`' own self-referential `parent_category_id` (PROMPT 10) had never attempted a PostgREST embed either, fetching flat and building the tree client-side, which is presumably why this limitation was never hit before. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/531 unit tests/build all clean) and live in the running app: a health policy insuring two people (both correctly listed), a recorded premium payment (correctly appears as an expense transaction linked to the policy), and a renewal — the *old* policy row's detail page showed its original ₹12,000/2026-dated period completely unchanged (just relabeled "Renewed (superseded)", with a link to the new period), while a separate new ₹13,500/2027-dated row appeared active and alone counted toward "Active policies"/"Total annual premium" — zero console errors throughout |
| Insurance claims & dashboard (`/app/insurance/dashboard`, claims on `/app/insurance/[policyId]`) | Implemented (PROMPT 26): `insurance_claims` — policy, insured person (must be one of that policy's own `insurance_policy_insured_people` rows, enforced by trigger), incident/claim dates, claimed and approved amounts, the full 8-value status list (preparing/submitted/information_requested/approved/partially_approved/rejected/paid/closed), hospital/provider, reference number, notes, and documents — full CRUD (`src/features/insurance/claims-actions.ts`/`claims-queries.ts`) plus a "Claims" table on each policy's detail page. **"Claim payment is not treated as normal income unless reporting deliberately categorizes it"**: `recordClaimSettlementAction` calls `record_insurance_claim_settlement()` (SECURITY INVOKER RPC, same "pair a status change with its transaction" shape as `record_loan_payment`/`record_lending_repayment`), which atomically writes a dedicated `kind = 'insurance_claim_settlement'` transaction (`transactions.insurance_claim_id`) — never `income` — and marks the claim `paid`; `status = 'paid'` is structurally only ever set by that RPC, never a bare status edit (`insurance_claims_settlement_shape` CHECK enforces the settled fields and `paid` status can only exist together). `cash_flow_transactions` stays `kind in (income, expense)` unchanged, so the settlement is excluded from every cash-flow/income figure by construction — zero reporting-layer changes needed. **"Policy history remains intact"**: `insurance_claims.policy_id` is `ON DELETE RESTRICT` (unlike the insured-people join's `CASCADE`), so a policy with claim history can never be hard-deleted out from under it; claims never write to `insurance_policies` at all. **"Claim documents are authorized"**: `attachments.attachable_type` grew an `'insurance_claim'` branch (same private-bucket, household-scoped-RLS, signed-URL-only guarantee every other attachable type has) — paired with a real working upload widget in `ClaimDialog` (mirroring `ExpenseDialog`'s receipt-upload pattern exactly: the browser uploads bytes directly to the private `documents` Storage bucket, the server action only records the resulting path), unlike `insurance_policy`'s own documents field, which PROMPT 25 left schema-only. **"Renewal reminders work"**: reuses PROMPT 25's `isRenewalDueSoon`/`getInsuranceOverview` machinery unchanged, now also surfaced on the dedicated dashboard. **Waiting-period milestones**: a new structured `insurance_policy_waiting_periods` table (label, `duration_months`, `starts_from`) supplements — never replaces — the existing freeform `waiting_periods` narrative text; its milestone/end date is always computed (`starts_from + duration_months`, `src/lib/calculations/insurance.ts`'s `computeWaitingPeriodMilestoneDate`, unit-tested), never stored, and rendered with a "Passed"/"Coming up" badge (`isWaitingPeriodPassed`/`isWaitingPeriodMilestoneUpcoming`, mirroring `isRenewalDueSoon`'s own advisory-only convention) on both the policy detail page and the dashboard. **Dashboard** (`src/features/insurance/dashboard-queries.ts`'s `getInsuranceDashboardData`, mirroring PROMPT 22's separate-route pattern — `/app/insurance` stays the manage list, `/app/insurance/dashboard` is the fuller reporting view, linked from the manage page's header the same way `/app/debts` links to `/app/debts/dashboard`) computes all 8 requested views from real stored data: active policies, total annual premium commitment, upcoming renewals, expired policies (a literal `status = 'expired'` filter, never inferred from a passed date), people without recorded health coverage, total coverage by person (active policies only, scoped to one currency — never blended, same convention as every other cross-policy total in this module), claims missing documents (reusing the same batched, no-N+1 document-count query the claims table itself uses), and waiting-period milestones. **"Without recorded coverage" must not claim the person is definitely uninsured outside DhanOS"**: the dashboard card carries a standing disclaimer that this only means no active health policy is tracked for them *in DhanOS* — never a claim about their actual insurance status. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/538 unit tests/build all clean): a claim filed against a health policy's insured person, a document uploaded and viewed via a signed URL, a settlement recorded (confirmed to appear as its own transaction kind and correctly excluded from the dashboard's "Income this month" figure), a status change, a waiting period added with its milestone date computed and badged correctly, and every dashboard card populating from real data — zero console errors throughout |
| Assets (`/app/assets`, `/app/assets/[assetId]`) | Implemented (PROMPT 27): `assets` — movable (vehicle/machinery/jewellery/gold/laptop/phone/furniture/equipment/collectible), immovable (land/house/shop/commercial_property/agricultural_land), or business (machinery/inventory/ownership_interest/intellectual_property/equipment) property in one table discriminated by `asset_group` (`category` cross-validated against it — `'machinery'`/`'equipment'` deliberately valid under both movable and business, disambiguated by group), owner (a person), acquisition type/date/value, location, condition, income-generation notes, an optional related loan, an include-in-net-worth flag, and a liquidity classification (liquid/semi_liquid/illiquid) — full CRUD (`src/features/assets/actions.ts`/`queries.ts`) at `/app/assets` (+ detail). **"Ownership percentages are supported"**: `ownership_percentage` (0, 100] is always applied when computing this household's owned share of an asset's value (`src/lib/calculations/assets.ts`'s `computeOwnedValueMinorUnits`, unit-tested) — never assumed to be 100%, verified live with a 50%-owned asset showing half its latest value as "Owned value." **"Disputed or expected property is not presented as fully owned"**: `ownership_status` includes `disputed`/`expected` alongside confirmed/shared/transfer_pending/documentation_incomplete/unknown — `computeNetWorthContributionMinorUnits` returns 0 for either of those two statuses regardless of the separate `include_in_net_worth` toggle, and the asset detail page shows a standing warning banner whenever ownership is uncertain; the assets overview page keeps a "disputed/expected value" figure strictly separate from the real net-worth-contribution total, never blended in, mirroring PROMPT 24's confirmed/estimated liability split. **"Asset values use snapshots"**: `assets` itself carries no current-value column at all (same "no value column" shape as `investment_holdings`) — `create_asset()` (SECURITY INVOKER RPC) atomically writes the asset row and its first `asset_valuation_snapshots` row from the create dialog's "estimated current value"/"valuation date" fields; every later re-valuation is a plain insert via `recordAssetValuationAction` (a "Record valuation" button on the detail page), never an edit of the asset row or any prior snapshot — the full dated history renders on the detail page. **"Attached debt remains separate"**: `related_loan_id` is a plain optional cross-reference to `loans`; the linked loan's outstanding balance is computed independently by reusing `src/lib/calculations/loan-outstanding.ts` unchanged (same `computeLoanOutstanding`/`selectEffectivePayments` the debt dashboard uses) and rendered in its own "Related loan" card, side by side with — never subtracted from — the asset's own value figures, verified live with a vehicle asset linked to a real loan with two recorded payments. Documents: `attachments.attachable_type` grew an `'asset'` branch, paired with a real working upload widget in `AssetDialog` (same `ExpenseDialog`/`ClaimDialog` upload pattern: the browser uploads bytes directly to the private `documents` Storage bucket, the server action only records the resulting path). Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/549 unit tests/build all clean) and live in the running app: assets created across all three groups (a vehicle, a house, and business inventory), a 50%-owned jointly-owned asset correctly showing half value toward net worth, a disputed asset correctly excluded from the net-worth total while still fully visible in the list and its own detail page, a second valuation recorded and appearing in history alongside the first, a document uploaded and viewed via a signed URL, and a loan-linked asset showing the loan's real outstanding balance as a separate figure — zero console errors throughout |
| Property depth & asset income (property fields on `/app/assets/[assetId]` for immovable assets) | Implemented (PROMPT 28): `assets` grows 15 more nullable, UI-gated-to-immovable columns — `property_type` (finer-grained than `category`), `land_area`/`area_unit`, `title_status`, `mutation_status`, `original_owner`, `legal_heir_notes`, `rental_status`, `occupancy`, `encumbrance_status`/`encumbrance_notes`, `dispute_status` (independent of `ownership_status` — a confirmed-owned property can still have an active boundary dispute), `registration_details`, and `ownership_share_notes` (a *description* of how `ownership_percentage` was determined, e.g. a partition deed — never a second numeric share; the percentage itself stays the single source of truth for net worth, unchanged from PROMPT 27). **"Do not store precise location publicly"**: a new `location_precise` column is deliberately excluded from `listAssets`' query column list (`ASSET_LIST_SELECT` in `src/features/assets/queries.ts`) — the list page never fetches it at all, not just avoids rendering it (a Server Component's fetched row is serialized into the page regardless of what the UI shows), same "sensitive field, detail-view only" shape People's `birth_date`/`notes` already use; `AssetDialog` fetches it fresh via a dedicated `getAssetLocationPreciseAction` on every edit-open (regardless of whether it was opened from the list or detail page) so the field is never silently blanked out and overwritten with null just because a list-sourced row never carried it. **"Unverified valuations are labeled estimates"**: `asset_valuation_snapshots` grows a required `confidence` (verified/professional/informal_estimate/unverified, defaulting to `unverified`) and optional `appraiser`; `isValuationConfidenceVerified` (`src/lib/calculations/assets.ts`, unit-tested) renders an "Estimate" badge for anything short of `verified` — on both the valuation history table's own row and the asset's headline "Latest value" figure, never just buried in history. **"Income generated links to cash flow"**: `transactions` grows a nullable `asset_id` (valid only for `kind = 'income'`, enforced by a CHECK); `recordAssetIncomeAction` writes a real income transaction tagged to the asset (a single-table insert, no RPC needed) — a new "Income generated" card with a "Record income" button and history table on the asset detail page, reusing `listAssetIncomeTransactions`, so rental/asset income is genuinely visible in Cash Flow/Income reporting, never just a free-text note. **"Historical property values remain accessible"**: unchanged from PROMPT 27 — every snapshot stays permanently in the valuation history table, never overwritten. **"Shared ownership affects net-worth inclusion"**: reuses PROMPT 27's `computeNetWorthContributionMinorUnits` unchanged — no new mechanism needed, verified live again specifically for a property record. `create_asset()` (drop-and-recreate, same convention as every other grown RPC) now also accepts the property fields plus the initial valuation's confidence/appraiser. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/551 unit tests/build all clean): a house asset with full property details filled in, its precise location confirmed absent from the assets list page's data while visible on its own detail page, a valuation recorded with confidence = unverified (correctly badged "Estimate" on both the history row and the headline value) followed by one with confidence = verified (badge disappears), and rental income recorded against the asset and confirmed to appear as a real transaction — zero console errors throughout |
| Debt dashboard & prepayment analysis (`/app/debts/dashboard`) | Implemented (PROMPT 22): a reporting layer over the same `loans`/`loan_payments` tables PROMPT 21 built, no new schema. `src/lib/calculations/amortization.ts` extracts the amortizing-loan engine PROMPT 20's standalone loan-prepayment calculator used privately, so it can be shared with `src/lib/calculations/debt-prepayment.ts` (the dashboard's own prepayment simulator, comparing a real loan's current outstanding/rate/EMI against no-prepayment/one-time-prepayment/increased-EMI/regular-extra-payment scenarios — all four reduce to the same engine with a different effective payment or one-time prepayment map, never four separate loops) — `computeLoanPrepayment`'s public behavior and the row shape its own test file inspects were verified unchanged by the refactor. `src/lib/calculations/debt-metrics.ts` (totals scoped per PROMPT 22: outstanding/EMI-burden are active-loans-only "current burden," while original-principal/interest-paid/principal-repaid are lifetime totals across every loan regardless of status — a deliberate split so "how much do I currently owe" and "how much have I ever borrowed/repaid" don't get conflated) and `src/lib/calculations/debt-trend.ts` (household-wide monthly trend merging every loan's balance history onto one month axis, plus the EMI calendar) are all pure and unit-tested. **Overdue is computed, never stored**: `computeOverdueAmount` sums real effective payments against an expected-EMIs-so-far figure, returning a typed `not_active`/`emi_not_set`/`not_yet_due` reason instead of a fabricated number whenever the comparison wouldn't mean anything (PROMPT 22: "overdue status is based on actual payment records" / "missing interest details do not produce fake accuracy") — the EMI calendar applies the same rule per (loan, month), always showing `scheduledAmountMinorUnits` and `actualAmountMinorUnits` as two distinct fields, never merged ("paid and scheduled values are distinct"). Debt-to-income divides the EMI burden by the trailing 3 months' average cleared income (`getIncomeTrend`, reused from PROMPT 11) and returns `null` — not a misleading 0% — when there's no income to divide by. Five charts (outstanding balance curve, principal vs. interest paid, debt reduction, lender exposure — modeled on PROMPT 18's allocation-chart pattern — and debt by type) plus the EMI calendar table and the prepayment simulator are wired into one dashboard page, cross-linked from `/app/debts`. **Two real bugs caught and fixed during this build**: (1) `toMonthKey` (src/lib/dates) round-trips a date-only string through `parseISO` (local-midnight) and reformats in a fixed `UTC` timezone, silently shifting the date by a day in any positive-UTC-offset timezone including this app's own Asia/Kolkata default — `debt-trend.ts` never calls it on plain date strings, using pure string-slicing/integer month-key arithmetic instead; (2) the prepayment simulator's amount fields were parsed as already being in minor units (`Number(input)`) instead of decimal rupees, making every simulated amount 100x too small — fixed to use `parseDecimalToMinorUnits`, verified live (a ₹50,000 one-time prepayment on a real test loan correctly showed ₹9,760 interest saved / 4 months reduced, matching an independent manual calculation). Verified end to end against the local Supabase stack: real loan with two recorded EMI payments and one deliberately-missed month showed the exact expected metrics (outstanding, interest/principal paid, overdue amount matching the missed EMI, 19% debt-to-income against recorded income), all five charts, and both "one-time prepayment" and "increased EMI" simulator scenarios computing correctly with the lender-disclaimer shown unconditionally |
| Money drains (`/app/money-drains`, `/app/money-drains/[moneyDrainId]`) | Implemented (PROMPT 29): `money_drains` — a single register for subscriptions, memberships, vehicles, unused services, rented space, gadgets, maintenance-heavy assets, contractual commitments, and recurring fees, discriminated by `drain_type` — full CRUD (`src/features/money-drains/actions.ts`/`queries.ts`) plus manage-list-and-analysis in one page (`/app/money-drains`, same "one combined page" shape PROMPT 23's Lending module used) and a detail page. Deliberately a tracking/analysis layer, never a second ledger — it has no RPC and writes no transaction of its own. **"Recurring expenses remain connected to transactions"** (acceptance criterion): an optional `linked_recurring_rule_id` (-> `recurring_rules`) is how a drain stays tied to its real generated/reconciled transactions — the detail and list views resolve that rule's actual current amount via `resolveAmountForDate` (`src/lib/calculations/recurring-schedule.ts`, the exact pipeline the Recurring feature itself uses) and show it plainly alongside — never blended into — the household's own entered `cost_amount_minor_units` estimate, so a drift between "what you told us" and "what the ledger actually shows" stays visible. An optional `linked_asset_id` (-> `assets`) similarly surfaces that asset's own latest `asset_valuation_snapshots` row rather than duplicating a second value history; an optional `linked_account_id` records where it's paid from. **"Estimated usage is visibly user-entered"**: `usage_frequency` has no default at the schema layer — every row requires an explicit choice — and every render labels it "your estimate," never presenting it as measured. **Analysis is factual and explainable** (`src/lib/calculations/money-drains.ts`, pure and unit-tested): `cost_frequency` (monthly/quarterly/half_yearly/yearly/one_time/irregular) is normalized onto an annualized/monthly-equivalent axis the same way PROMPT 17's `sip-commitment.ts` and PROMPT 25's `annualizePremiumMinorUnits` do — `irregular` deliberately returns `null` rather than a guessed number, so an irregular-cost item is always excluded from a total and counted separately (`irregularCostCount`), never silently absorbed. The overview page computes total monthly/annual drain, an essential-vs-discretionary split, unused subscriptions (never/rarely used), high-cost-low-use (at or above the household's own average monthly drain cost, itself derived from real entered data rather than a hardcoded currency-dependent threshold), upcoming renewals (within 30 days, purely advisory), maintenance burden (vehicles + maintenance-heavy assets), and depreciating-assets-linked (cross-referencing each linked asset's latest tracked value). **"Do not automatically order the user to cancel anything"**: every one of those views is descriptive only — nothing in this feature ever changes `status` on the household's behalf; `setMoneyDrainStatusAction` (active/paused/cancelled) is always a deliberate, explicit user action, and a cancelled item is never deleted so its historical cost stays visible. Unlike most financial records in this app, a genuinely mistaken entry can be hard-deleted (owner/admin only, `deleteMoneyDrainAction`) since nothing else references a `money_drains` row — the normal way to record "I actually stopped this" is marking it cancelled, not deleting it. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/576 unit tests/build all clean) and live in the running app: a subscription created with a monthly cost and "rarely" usage immediately appeared correctly in the manage table and in both the "Unused subscriptions" and "High-cost, low-use" analysis cards, its monthly/annual totals matched the entered cadence exactly, editing it and changing its status to "Paused" both persisted and rendered correctly on the detail page, and the "Money Drains" nav item and both routes work — zero console errors throughout |
| Financial goals (`/app/goals`, `/app/goals/[goalId]`) | Implemented (PROMPT 30): `goals` — 15 goal types (emergency fund, house construction/purchase, land purchase, sister's/personal marriage, education, business launch, vehicle, healthcare reserve, parents' retirement, travel, renovation, debt closure, custom) with target amount (today's purchasing power), currency, target date, required inflation/expected-return assumptions, priority, flexibility, and a manual untracked-savings figure — plus `goal_responsible_people` (many-to-many to `people`, same shape as `insurance_policy_insured_people`) and `goal_funding_sources` (one row per linked `financial_accounts`/`investment_holdings` row, each with its own `allocation_percentage`). `create_goal()` (SECURITY INVOKER RPC, same "array + jsonb-array parameter" idiom as `create_insurance_policy`) atomically writes a goal plus its initial responsible-people/funding-source rows; adding/removing a single funding source or responsible person afterward (`src/features/goals/actions.ts`) is a plain single-table write, no RPC needed. **Goal math reuses PROMPT 20's calculator directly**: `src/lib/calculations/goals.ts` builds on `src/lib/calculations/calculators/goal-funding.ts`'s `computeGoalFunding` (months remaining, inflation-adjusted target, required monthly contribution, projected value) rather than re-deriving the same future-value arithmetic, adding only what a *persisted* goal needs on top — `computeGoalCurrentSavedAmount` (the manual figure plus every funding source's real current value, reusing `listAccounts`' calculated balance and `getPortfolioHoldings`' latest valuation directly rather than a second value pipeline, excluding a currency-mismatched source rather than blending it), `computeGoalFundingGapMinorUnits`, and `computeGoalOnTrackStatus` (funded/on_track/needs_contribution/overdue — each state traceable to a real comparison against the goal's own stated assumption, never an opaque verdict — PROMPT 30: "never assume investment returns are guaranteed"). **"Goals can have multiple funding sources"** (acceptance criterion): a goal's saved amount is never the manual figure alone. **"The same investment allocation cannot be accidentally counted fully toward several goals without showing the overlap"**: `computeFundingSourceAllocationTotals` sums a source's `allocation_percentage` across *every* goal linking to it (fetched household-wide, not just the current goal), and `isFundingSourceOverAllocated` flags a total over 100% — surfaced as a visible warning on every goal that source touches, never blocked and never hidden. **"Assumptions are visible"**: `annual_inflation_rate`/`annual_expected_return` are required columns, always rendered in a standing "Assumptions used in these figures" panel next to every derived number. **"Calculations have tests"**: `src/lib/calculations/goals.ts` and the reused `goal-funding.ts` are both pure and unit-tested. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/588 unit tests/build all clean) and live in the running app: an emergency-fund goal created with a manual saved amount correctly showed its inflation-adjusted target, funding gap, required monthly contribution, and "Needs contribution" status; linking a real test account to two different goals at 70% and 50% correctly summed to a 120% total and rendered an "Over-allocated" warning on *both* goals' detail pages — zero console errors throughout |
| Emergency fund planner (`/app/emergency-fund`) | Implemented (PROMPT 31): `emergency_fund_plans` — exactly one per household (`unique (household_id)`) — storing only `coverage_target_months` (user-selected) and `dependants_count` (used only to prefill a *suggested* target via `suggestCoverageTargetMonths`, never to override the household's own choice), plus `emergency_fund_source_overrides` (a household's explicit deviation from a real account/holding's structural default classification). Everything else is derived fresh on every read, reusing each domain's own existing query rather than re-deriving it: average essential monthly expenses (trailing 3 months, `getExpenseSummary` from Expenses — matching the debt dashboard's own trailing-3-months convention), monthly EMIs (`loans.emi_amount_minor_units`, active loans only), and insurance commitments (`getInsuranceOverview` from Insurance, annualized premium ÷ 12). **Coverage math** (`src/lib/calculations/emergency-fund.ts`, pure and unit-tested — satisfying "coverage calculation is tested"): monthly burn rate (essential expenses + EMIs + insurance), target amount (burn rate × coverage months), months of coverage and progress percentage (both `null` rather than a fabricated ratio when the denominator is zero), shortfall (floored at zero, can show "overfunded" via a progress percentage over 100%), and a suggested monthly contribution to close any shortfall over a 12-month horizon — never a projected return, unlike Goals, since an emergency fund is by definition money you can already reach. **"Included assets are transparent" / "user can override inclusion"**: every qualifying `financial_accounts`/`investment_holdings` row is always listed with its computed default classification and an explicit reason (`classifyAccountLiquidity`, new; `classifyLiquidity` from `portfolio-performance.ts`, reused unchanged for investments) — only a `liquid` classification defaults to included; toggling any source's checkbox writes/clears an override and a "Reset to default" control appears only once one exists. **"Do not count" is satisfied structurally**: `assets` and `lendings` are never queried by this feature (illiquid property, disputed receivables, and uncertain inherited assets are simply never candidates); `financial_accounts.account_type in ('loan', 'credit')` is never offered as a candidate at all — no override exists for a credit limit, since it's a liability, not owned money; retirement accounts (provident_fund/pension) and fixed deposits default to excluded but remain overridable, matching the prompt's "by default" wording. **"UI explains why liquidity matters"**: a standing "Why liquidity matters" panel explains the liquid/semi-liquid/illiquid distinction and why each excluded category doesn't count, in plain language, on every visit to the page. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/603 unit tests/build all clean) and live in the running app: creating a plan with 2 dependants correctly suggested a 5-month coverage target; a real savings account (`liquid`) defaulted to included and a provident-fund account (`illiquid`) defaulted to excluded, both with their reason text shown; unchecking the savings account correctly zeroed "Liquid emergency money" and revealed a "Reset to default" control, which correctly restored the original ₹2,50,000 figure — zero console errors throughout |
| Net-worth engine (`/app/net-worth`) | Implemented (PROMPT 32): grows the tenancy-era `net_worth_snapshots` table (created in the very first migration with only `total_assets_minor_units`/`total_liabilities_minor_units`, never written to by any feature until now) with the full component breakdown — **"do not store only the final total"**. Every component reuses another domain's own already-correct logic rather than re-deriving it: eligible `financial_accounts` balances (excludes `account_type in ('loan', 'credit')`, same convention as the emergency fund planner — PROMPT 31); `investment_holdings`' latest valuations (`getPortfolioHoldings`); `assets`' ownership-adjusted net worth contribution (`computeNetWorthContributionMinorUnits`, PROMPT 27 — **"ownership percentage is applied correctly"** was already solved there, this is its first real consumer), split by `asset_group` into movable assets and property; `lendings`' currently-owed outstanding as receivables (`computeLendingTotals`); `loans`' active outstanding as institutional debt (`getDebtSummary`); and `liabilities` split into informal debt (any certainty) plus general obligations with `certainty = 'confirmed'` only (`src/lib/calculations/net-worth.ts`'s new `computeOtherLiabilitiesBreakdown` — an *estimated* general obligation is deliberately excluded, surfaced separately, never silently folded in). `total_assets_minor_units`/`total_liabilities_minor_units`/`net_worth_minor_units` are all `generated always as (...) stored not null` columns — they can never drift from their components, the same guaranteed-consistent idiom as `account_balance_snapshots.difference_minor_units`. **"Missing valuations lower completeness rather than becoming zero silently"**: `completeness_percentage`/`valuation_dependent_item_count`/`missing_valuation_count` are computed and stored with every snapshot (100% when there are no valuation-dependent items at all, never a divide-by-zero) — a missing valuation still contributes 0 to its component, but completeness always visibly reports what fraction of holdings/assets actually had one, and the dashboard explains this explicitly rather than letting the total look more reliable than it is. **"Snapshot values are reproducible"**: deliberately scoped to "as of now" only — `src/features/net-worth/queries.ts`'s `getCurrentNetWorthBreakdown` never backdates a recomputation, so recomputing from the same underlying data always yields the same breakdown, without needing every domain module to support point-in-time-in-the-past queries. **"Historical snapshots are not rewritten automatically"**: already true structurally (append-only, no update grant, `unique (household_id, as_of_date)`); `recordNetWorthSnapshotAction` explicitly checks for an existing snapshot on today's date first and returns a clear message rather than relying on a raw constraint-violation fallback. Five charts (`src/features/net-worth/charts/`) — net-worth curve, asset growth, debt reduction, contribution vs. valuation change (investments only, decomposing the period's value change into real net contributions from `investment_transactions` versus market movement, mirroring PROMPT 18's principal-vs-growth stacked-bar pattern), and month-over-month change (sign-aware bars, same convention as the dashboard's `SavingsTrendChart`) — are built entirely from real recorded snapshots, never interpolated or projected for a period with no snapshot. The dashboard shows the live (not-yet-recorded) breakdown by default, with standing explanations of why eligible-account filtering and ownership percentage matter, and a "Record snapshot" button that's disabled once today's snapshot already exists. `src/lib/calculations/net-worth.ts` is pure and unit-tested (16 tests). Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/619 unit tests/build all clean) and live in the running app: a fresh household showed ₹0 everywhere with 100% completeness (correctly, since there was nothing to be incomplete about); adding a real savings account immediately raised cash & accounts and net worth by the exact deposited amount, while a credit-card account with a real balance was correctly excluded entirely (neither an asset nor a liability, per PROMPT 32's formula) — zero console errors throughout |
| Financial documents vault (`/app/documents`) | Implemented (PROMPT 34): a household-wide, browsable document store — the new `documents` table (17-value `category` taxonomy: bank statement/salary slip/loan agreement/education-loan document/insurance policy/premium receipt/claim document/investment statement/tax document/property paper/valuation report/lending agreement/nominee record/invoice/receipt/identity-related document/other) is deliberately separate from the pre-existing `attachments` table (PROMPT 9) rather than an extension of it — `attachments` stays exactly what it was, a lightweight file glued to one specific entity row (a transaction's receipt, an asset's deed), while `documents` carries fields attachments was never built to hold (`document_date`/`expiry_date`, a user-toggleable `status`, a `checksum`) and is frequently *not* tied to any entity at all (a PAN card, a will). `entity_type`/`entity_id` is a loose, informational link — unlike `attachments.attachable_type`/`attachable_id`, it is deliberately not trigger-verified against the referenced table, since the vault's entity set is open-ended rather than attachments' small closed set. Reuses the existing private `documents` Storage bucket and its household-scoped `storage.objects` RLS from PROMPT 9 unchanged — those policies only ever trust the path's first segment (the household id), which the vault's own path convention (`householdId/entityType/entityId/documentId/originalFilename`, using the literal segment `general` and the household id again when a document has no entity link) still satisfies. Security properties: RLS scopes every row/object read+write to household membership (`select` for any active member; `insert`/`update` for owner/admin/editor; **`delete` restricted to owner/admin only** — "deliberate permanent deletion" — archiving, an ordinary editor-accessible update, is the reversible alternative); every download is a signed URL minted server-side after `requireHouseholdRole` re-checks membership against the row's own `household_id` (never the client-supplied path), with a deliberately short 120-second TTL; a client-side MIME allowlist + 25 MB size cap (also re-validated server-side in `createDocumentSchema`) rejects unsupported files before they ever reach Storage; a failed metadata-insert after a successful Storage upload triggers a best-effort client-side cleanup removing the orphaned object; and an optional SHA-256 checksum is computed client-side via `crypto.subtle` before upload. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/646 unit tests/build all clean) and live in the running app plus a direct Supabase-client script: uploaded a file and created its metadata row, confirmed an unauthenticated client gets `permission denied for table documents` and the bucket refuses a public URL (400), viewed/downloaded via a real signed URL, archived (excluded from the default list, included via the "Include archived" filter), edited metadata, and permanently deleted — confirming both the row and the Storage object were gone afterward, zero orphaned state |
| Financial decision journal (`/app/decisions`, `/app/decisions/[decisionId]`) | Implemented (PROMPT 37): one row per significant financial decision (starting a SIP, lending money, taking an education loan, buying insurance, purchasing an asset, pausing an investment, a loan prepayment), capturing the reasoning *at the time*, not just the outcome. **"Original rationale remains preserved"** is enforced at the database layer, not just by UI convention: `enforce_decision_journal_immutability()` (a `before update` trigger) rejects any change to title/decision_date/amount/currency/entity link/context/choice/alternatives/rationale/expected_result/risks once a row exists — only `status`/`review_date`/`actual_outcome`/`lessons_learned` may ever change afterward (verified directly: an UPDATE attempting to rewrite `rationale` was rejected with the trigger's own exception message, while a `status` update on the same row succeeded). `entity_type`/`entity_id` is a closed, seven-value, trigger-verified set (financial_account/investment_sip/loan/lending/asset/goal/insurance_policy — same shape as `attachments`/`reminders`, not `documents`' looser pair) — chosen deliberately broader than the field list's literal five examples (account/investment/loan/asset/goal) specifically so "lending money" and "buying insurance," two of PROMPT 37's own named examples, have a real entity to link to. **"Outcome can be recorded later"**: `actual_outcome`/`lessons_learned` are nullable and only ever set by `recordOutcomeAction`, a plain single-column update wholly independent of the write-once fields (verified live: recorded an outcome and lessons-learned on a decision hours after creating it). **"Review date creates a reminder"**: extends PROMPT 35's reminders feature with a 13th type, `decision_review` — `reminders.reminder_type`/`entity_type` CHECK constraints widened (drop + re-add, the same convention `attachments.attachable_type` has now used five times) and `check_reminder_target()` grown a `decision_journal_entry` branch; `generateDecisionReviewCandidates` (`src/lib/calculations/reminders.ts`) only surfaces a reminder while status is `decided`/`under_review` (an `'open'` draft was never finalized; a `reversed`/`superseded` entry has nothing left to review) — verified live: setting a review date produced a real "Decision review" reminder on `/app/reminders`, correctly labeled with the decision's own title and linking back to it. **"Superseding does not erase history"**: `create_decision_journal_entry()` (SECURITY INVOKER RPC, mirroring `create_insurance_policy()`'s renewal pattern exactly) atomically inserts a brand-new entry and, when `p_supersedes_entry_id` is given, flips the *older* entry's status to `'superseded'` in the same call — the old row's own content is never touched by any UPDATE, ever (the immutability trigger would reject it regardless). A partial unique index (`supersedes_entry_id where not null`) keeps the supersession chain a clean linked list, never a fork. Verified live end to end: superseded a decided decision with a revised one, then confirmed the *original* entry still showed its exact original title/context/choice/rationale, now `status = 'superseded'`, with a note pointing to the new entry — and the new entry showed a note pointing back, with its own (different) choice text. No edit action exists anywhere in the UI for a decision's write-once fields — only `DecisionDialog`'s create/supersede paths ever call the insert RPC. Deletion is restricted to a still-`'open'` draft only (owner/admin, enforced both by the delete action's own `WHERE status = 'open'` clause and by RLS) — a decided/reversed/superseded entry can never be deleted, only superseded or marked reversed. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/700 unit tests/build all clean) and live in the running app |
| Reports / reporting centre (`/app/reports`, `/app/reports/[slug]`) | Implemented (PROMPT 36): 17 reports (monthly cash flow, income by source, expense by category, essential vs. discretionary, investment contributions, portfolio allocation, portfolio growth, SIP consistency, staking performance, debt position, debt reduction, insurance coverage, assets and liabilities, lending exposure, goal readiness, emergency-fund coverage, net-worth history), each a chart + a reconciling, exportable table. Deliberately a **reuse-first** feature, not a reimplementation: `src/features/reports/queries.ts`'s 17 fetch functions call an already-existing feature query/calculator for all but one figure (`computeSipConsistency`, `src/lib/calculations/reports.ts` — the one genuinely new metric, since no table stores a history of every past SIP due date and whether it was met; it re-derives expected occurrences by reusing `enumerateOccurrencesInWindow`, the same schedule-stepping primitive PROMPT 35's reminders already established, and compares that to actually-recorded contributions), and 10 of the 17 charts reuse an *existing* chart component verbatim (`ExpenseCategoryChart`, `AllocationChart`, `PrincipalVsGrowthChart`, `ContributionHistoryChart`, `ActualVsExpectedChart`, `DebtGroupChart`, the loans (not net-worth) `DebtReductionChart`, `BorrowerExposureChart`, `NetWorthCurveChart`, `CashFlowBreakdownChart`) rather than being rebuilt — the other 7 (income by source, essential vs. discretionary, SIP consistency, insurance coverage, assets and liabilities, goal readiness, emergency-fund coverage) are new but built on `ChartCard` (PROMPT 15) exactly like every other chart in the app. **Chart rules**, all satisfied structurally rather than per-chart: every report's title *is* the specific question it answers (`ReportDefinition.question`, shown directly under the title); `ChartCard` already renders the date-range/data-cutoff caption, the accessible summary, and the empty state (so "no decorative fake data" holds — a report with nothing to show renders `isEmpty`, never a fabricated bar); axes always start at 0 (recharts default, never overridden) — "avoid misleading truncated axes"; actual-vs-projected (`ActualVsExpectedChart`, SIP consistency's expected-vs-completed grouped bars) and principal-vs-growth (`PrincipalVsGrowthChart`) both reuse their existing established visual patterns; every new chart sets `isAnimationActive={false}` on its data series. **"Chart totals reconcile with tables"**: every report builds its `tableRows`/`csvRows` from the exact same array the chart itself renders (`src/features/reports/types.ts`'s `ReportTableData`) — never a second, independently-fetched computation — so reconciliation is structural, not a matter of careful bookkeeping (verified live: seeded one real expense, both the chart's accessible summary and the table row showed the identical ₹1,500.00). **"Exportable table view exists where practical"**: `src/lib/reports/csv.ts`'s `buildCsv` (pure, unit-tested) + `ExportCsvButton`, on every one of the 17 reports — the first CSV/file-export capability anywhere in this codebase. **"Privacy mode applies to charts and tooltips"**: every chart already conceals via `ChartCard`; `ReportTable`'s money cells go through `SensitiveAmount` (extending the rule to the table too, since a plain-text table sitting next to a concealed chart would otherwise defeat it); `ExportCsvButton` is deliberately *disabled* while concealed (verified live: `disabled: true`, `title: "Turn off privacy mode to export"") — real figures never leave the concealed page via a side channel. **Shared filters**: date range, person, institution, category, and asset class are wired into whichever reports can cleanly support them (`ReportDefinition.relevantFilters` — a report only ever shows the filter controls it actually acts on, never a decorative one that silently does nothing); `account` and `verified/manual/imported source` exist in the shared filter vocabulary (`src/lib/validation/reports.ts`) but aren't wired into any report's query yet — a disclosed scope limit, not a silent gap. No new persisted table: unlike the `reports`/`data_cutoff_at` entity `database-plan.md` had speculatively anticipated, every report here is computed live from real, current (or real, historical-snapshot) data, the same "compute fresh, never store a redundant copy" convention net-worth/goals/emergency-fund already established — `dataCutoffLabel` states "today" for a live computation or "from recorded snapshots only" for net-worth history, exactly the way `net_worth_snapshots.source_cutoff_at` already distinguishes data-cutoff from generated-at. Verified end to end against the local Supabase stack (generated types unchanged — no schema migration needed — typecheck/lint/696 unit tests/build all clean) and live in the running app: all 17 reports render on the hub grouped by category; a report with zero data shows real empty states in both chart and table (never fake placeholder bars); a report's filter bar shows only its own relevant controls (confirmed on `portfolio-allocation`, asset-class only, and `insurance-coverage`, institution + policy-type only); privacy mode concealed both the chart and the table cell and disabled the export button; and a seeded real expense transaction produced matching, reconciling figures in both the chart's accessible summary and the table |
| Reminders / financial calendar (`/app/reminders`) | Implemented (PROMPT 35): 12 reminder types (SIP due, EMI due, insurance premium, policy renewal, loan review, expected income, lending repayment, document expiry, fixed-deposit maturity, goal review, monthly closing, asset valuation review) generated from each source module's own dates — never a second, independently-entered schedule. `src/lib/calculations/reminders.ts` is pure schedule/classification math (fully unit-tested): `classifyReminder` derives upcoming/overdue/snoozed/completed/skipped from `status`/`due_date`/`snoozed_until` against an explicit `asOfDate`, and `enumerateOccurrencesInWindow` reuses `computeNextOccurrenceAfter` (PROMPT 14's recurring-schedule math) for every fixed-cadence type (EMI, loan review, insurance premium, goal review, asset valuation review) rather than reimplementing month/quarter/year stepping. Where no existing field captures a type's due date — loan review, goal review, asset valuation review, monthly closing (and `financial_accounts.maturity_date`, a new nullable column for fixed-deposit maturity) — this PROMPT introduces one, documented inline as an explicit assumption (e.g. an annual loan/valuation review, a quarterly goal review) rather than a hidden default. **"Reminder completion and payment remain separate"**: the new `reminders` table only ever tracks acknowledgement (`pending`/`completed`/`skipped`, `snoozed_until`) — `completeReminderAction`/`skipReminderAction`/`snoozeReminderAction`/`reopenReminderAction` each touch exactly this one table, with no code path into any source table at all (verified directly: completing a reminder left every source row's `updated_at` and row count byte-for-byte unchanged). **"Recurring reminders do not duplicate"**: a `unique (household_id, reminder_type, entity_type, entity_id, due_date)` constraint backs every generated occurrence, upserted with `ignoreDuplicates` — the same dedup shape as `transactions_recurring_rule_occurrence_uidx` (PROMPT 14) — verified by reloading the page (which re-runs generation) and confirming the row count didn't change. **"Timezone and date-only obligations behave correctly"**: a new `getTodayInTimeZone(timezone)` (src/lib/dates) computes "today" in the household's own `households.timezone` — previously a dormant, unused column — rather than the server's UTC clock, the first feature to actually wire it into a due/overdue computation; every due date stays a plain date-only string throughout, never a timestamp. `entity_type`/`entity_id` is a closed, ten-value, trigger-verified set (mirroring `attachments`, not `documents`' looser pair) since every reminder genuinely originates from one real row — except `entity_type = 'household'` (monthly closing, which has no row to point at since the closing doesn't exist yet), using `entity_id = household_id` as a self-reference so the dedup constraint never has to special-case a NULL column. Generation (`src/features/reminders/sync.ts`) runs best-effort on every page load and via a manual "Refresh" button (`syncRemindersAction`), bounded to a [today − 30 days, today + 90 days] window — never an unbounded historical scan. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/681 unit tests/build all clean) and live in the running app with seeded data across 10 of the 12 types (SIP due and monthly closing's "no data needed" case were exercised via unit tests instead): generation produced exactly the expected due dates (including a quarterly insurance premium and a monthly EMI correctly landing in Overdue), completing/snoozing/skipping/reopening each worked and correctly moved reminders between views, a direct duplicate-upsert attempt against the dedup constraint left exactly one row, and completing a reminder was directly confirmed (via a before/after snapshot of every source table) to leave every source table completely untouched |
| Monthly financial closing (`/app/monthly-closing`, `/app/monthly-closing/[closingId]`) | Implemented (PROMPT 33): realizes the `monthly_closings` entity already anticipated (but unbuilt) since the earliest planning docs. `start_monthly_closing()` (SECURITY INVOKER RPC) atomically creates a closing plus its 12-item review checklist (account balances, income, expenses, transfers, SIP contributions, investment valuations, loan balances, lending repayments, insurance premiums, asset changes, goals, unusual transactions). Completing a closing is a single narrow update that freezes income/expense/investment-contribution/debt-payment totals — reusing the dashboard's `getCashFlowSummary` directly, never re-derived — and links a net-worth snapshot, ensuring one exists "as of now" via the net-worth engine (PROMPT 32) exactly as `recordNetWorthSnapshotAction` does; `net_cash_flow_minor_units` is a generated column. **"Closed month remains viewable"**: nothing ever deletes a closing; the current one for a period is simply whichever row has the latest `created_at` in that period's correction chain — no forward-pointer column needed. **"Later corrections are marked"**: re-closing a reopened period always inserts a brand-new row (`supersedes_closing_id`, incremented `report_version`) — the old row's frozen totals are never rewritten, and it permanently keeps `status = 'reopened'` as a historical fact. **"Reopening requires deliberate confirmation"**: `ReopenClosingDialog` (mirroring liabilities'/lending's own reversal-reason dialogs) always requires a typed reason before submitting, and the action only ever updates `status`/`reopened_at`/`reopened_by`/`reopen_reason`. **"Reports state when data is incomplete"**: `src/lib/calculations/monthly-closing.ts`'s `computeClosingCompleteness` (pure, unit-tested) always gives a specific, countable reason — N of M review items unresolved, or the linked net-worth snapshot's own completeness below 100% — rendered as a standing banner, never an opaque flag. The monthly report additionally pulls major/unusual expenses (`getLargestExpenses`), missed commitments (`getMissedRecurringRules`), goal progress (`listGoals`/`getGoalsOverview`), and upcoming obligations (recurring commitments, insurance renewals, money-drain renewals) directly from each module's own existing queries — no new aggregation logic duplicated. Verified end to end against the local Supabase stack (`pnpm db:reset`, generated types, typecheck/lint/631 unit tests/build all clean) and live in the running app: starting a closing created all 12 checklist items; marking 3 of 12 reviewed and completing anyway correctly produced a "9 of 12 review item(s) were not marked reviewed" incompleteness banner rather than pretending the review was finished; reopening was blocked with no reason given, then succeeded once one was typed, correctly showing a "Reopened" badge with the reason and date while every original frozen figure stayed exactly as first recorded — zero console errors throughout |
| Unit/component tests | Vitest + React Testing Library, co-located `*.test.ts(x)` files, 631 passing |
| E2E tests | Playwright: `tests/e2e/smoke.spec.ts` (routes not requiring a session), `tests/e2e/auth.spec.ts` (full sign-up/sign-in/reset/sign-out flows against the real local Supabase stack, including a real emailed password-reset round trip via local Mailpit — see `tests/e2e/support/mailpit.ts`), `tests/e2e/household-isolation.spec.ts` (cross-tenant isolation + duplicate-onboarding-retry, via direct REST calls with real access tokens — see `tests/e2e/support/supabase-rest.ts`), `tests/e2e/shell.spec.ts` (unauthorized nested-route access, direct-load/refresh of a nested route, privacy mode across every dashboard card + surviving a refresh, mobile nav, no console errors, no amount in the document title), `tests/e2e/core-ledger.spec.ts` (core-ledger schema: invalid currency/transaction-kind rejection, cross-household isolation for accounts/transactions, transfers structurally excluded from `cash_flow_transactions`, `transaction_splits` sum-total integrity, append-only tables reject update/delete — re-verified passing after the PROMPT 10 through PROMPT 13 schema changes), and `tests/e2e/people-institutions.spec.ts` (People/Institutions enum/format rejection, cross-household isolation, exact-name-duplicate rejection, archiving), 41 passing |
| Production build | `pnpm build` succeeds; combined `pnpm verify` runs typecheck → lint → format:check → unit tests → build |
| Deployment | Not configured |
| Environment variables | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_APP_URL`, `SUPABASE_SERVICE_ROLE_KEY` — validated, documented in `.env.example` |

## 2. Module status (against [product-scope.md](./product-scope.md) §3)

Every module below except Authentication and the household model is **not started**. This table exists to be checked off, not to imply hidden progress:

- [x] Authentication
- [x] Personal financial workspace / household model
- [ ] Family and household members — the tenant boundary, roles, and status exist; the `people` table (with a full CRUD UI at `/app/people`) now represents non-login household members (dependants, lenders, nominees) for ownership/attribution purposes, but inviting/managing *other login* users into a household has no flow yet (deliberately deferred — see PROMPT 4)
- [x] Institutions and financial platforms — full CRUD UI at `/app/institutions`, including the duplicate-warning flow
- [x] Accounts and balances — full CRUD + detail UI at `/app/accounts`, calculated-balance strategy, and an auditable manual-correction flow (see PROMPT 9 row above)
- [x] Income sources — dedicated `income_sources` table with full CRUD UI at `/app/income` (+ detail): expected-vs-received comparison, missed-income indicator, income by source/by person, income trend, and duplicate-recurring-receipt warning (see PROMPT 11 row above)
- [x] Expenses — full CRUD at `/app/cash-flow` (add/edit/archive/refund/split, search, filters, pagination — PROMPT 10) plus a dedicated views/analysis layer at `/app/expenses` (essentials/discretionary/irregular/recurring/unplanned/by-person/by-merchant/by-category views; category totals, essential/discretionary/unplanned amounts, average daily spend, month-over-month comparison, largest expenses, all net of refunds — see PROMPT 12 row above)
- [x] Transfers — dedicated one-form UI at `/app/transfers` (PROMPT 13): fees, pending/cleared status, any account-type pairing, same-account rejection, explicit cross-currency conversion (never an invented rate), reversal, and archive — all with balanced source/destination effects — plus the pre-existing structural exclusion from `cash_flow_transactions` and the general Cash Flow form's same-currency-only path (see PROMPT 13 row above)
- [x] Recurring commitments — full CRUD + lifecycle UI at `/app/recurring` (+ detail): daily/weekly/biweekly/monthly/quarterly/half-yearly/yearly/custom-interval frequencies, upcoming/missed commitments, pause/resume, skip one occurrence, dated future amount changes, auto-create (planned only, never cleared) or reminder-only behavior, and a full recurrence history — see PROMPT 14 row above. No scheduled/cron job exists yet to run `generateDueOccurrencesAction` automatically; it's triggered manually from the UI today
- [x] Investments — schema (PROMPT 16) + portfolio/performance reporting (`/app/investments/portfolio`, PROMPT 18): core metrics, XIRR, allocation, and gain/loss-by-holding — see the rows above. No dedicated CRUD UI for `investment_accounts`/`investment_assets` themselves yet (created inline from the SIP dialog only)
- [x] SIPs — full CRUD + lifecycle + contribution-recording UI at `/app/investments` (PROMPT 17) — see the SIPs row above
- [x] Staking positions — dedicated daily-value-tracking UI at `/app/investments/staking` (PROMPT 19): revision-versioned daily snapshots, closing-value balance validation, non-guaranteed projection charting, and risk treatment (high-return/concentration/staleness/manual-entry) — see the Staking row above
- [ ] Valuation history
- [x] Loans — full CRUD + disbursement/payment/reversal lifecycle at `/app/debts` (+ detail): 10 loan types, education-loan fields, derived (never stored) outstanding principal, explicit overpayment confirmation, EMI breakdown, and debt charts over actual payments/balances — see the Loans & debt row above; plus a dedicated debt dashboard and prepayment simulator at `/app/debts/dashboard` (PROMPT 22) — metrics, five charts, an EMI calendar, and a four-scenario prepayment comparison, all computed from actual payment history — see the Debt dashboard row above
- [x] Lending and receivables — full CRUD + repayment/reversal lifecycle at `/app/lending` (+ detail), plus total lent/outstanding, overdue/upcoming, borrower exposure, and recovery-history views on the same overview page — see the Lending & receivables row above (PROMPT 23)
- [x] Insurance — 12 policy types + health-specific fields, multi-person coverage, renewal-as-new-row lifecycle, and premium payments as real expense transactions at `/app/insurance` (+ detail) — see the Insurance row above (PROMPT 25); claims (status lifecycle, settlement transactions structurally never income, document uploads), structured waiting-period milestones, and a dedicated dashboard at `/app/insurance/dashboard` — see the Insurance claims & dashboard row above (PROMPT 26)
- [x] Movable and immovable assets — movable/immovable/business property in one register at `/app/assets` (+ detail), ownership percentage + confirmed/disputed/expected status (disputed/expected never counted toward net worth), dated valuation snapshots (never a mutable value field), an optional related loan with its outstanding always shown separately, and document uploads — see the Assets row above (PROMPT 27); property-specific fields (title/mutation/dispute status, land area, registration, rental/occupancy, legal-heir notes), a privacy-gated precise-location field never fetched by the list page, valuation confidence with "Estimate" labeling, and asset-generated income as real cash-flow transactions — see the Property depth & asset income row above (PROMPT 28)
- [x] Liabilities — informal borrowing (family/friend/employer advance/unpaid obligation/business borrowing/personal settlement) and general obligations (unpaid tax/pending bill/contractual commitment/guarantee/maintenance obligation/recurring commitment) in one register at `/app/liabilities` (+ detail), integrated with total debt (institutional/informal/general shown separately) and estimate-vs-confirmed certainty labeling — see the Informal borrowing & liabilities row above (PROMPT 24)
- [x] Money drains — depreciating and money-draining items (subscriptions, memberships, vehicles, unused services, rented space, gadgets, maintenance-heavy assets, contractual commitments, recurring fees) in one register at `/app/money-drains` (+ detail), connected to real transactions via an optional linked recurring rule and to real valuations via an optional linked asset, with factual/explainable analysis (annualized cost, unused, high-cost-low-use, upcoming renewals, maintenance burden) and no automatic cancellation — see the Money drains row above (PROMPT 29). Not in [product-scope.md](./product-scope.md)'s original module list; added directly per PROMPT 29
- [x] Future financial goals — 15 goal types with inflation-adjusted target amounts, required inflation/expected-return assumptions, priority/flexibility, multiple funding sources (linked accounts/investments with per-goal allocation percentages and cross-goal overlap detection), responsible people, and computed funding-gap/required-contribution/on-track figures at `/app/goals` (+ detail) — see the Financial goals row above (PROMPT 30)
- [x] Emergency-fund planning — one plan per household with a user-selected coverage target and dependants count, liquid emergency money/months of coverage/target amount/shortfall/progress/suggested monthly contribution computed from real essential expenses, EMIs, and insurance commitments, transparent per-source liquidity classification with household override, and a "why liquidity matters" explainer at `/app/emergency-fund` — see the Emergency fund planner row above (PROMPT 31)
- [x] Net-worth tracking — current (live) and historical (recorded-snapshot) net worth with a full component breakdown (cash/accounts, investments, movable assets, property, receivables, loans, other confirmed liabilities), completeness tracking for missing valuations, and five charts (net-worth curve, asset growth, debt reduction, contribution vs. valuation change, month-over-month) at `/app/net-worth` — see the Net-worth engine row above (PROMPT 32)
- [x] Monthly financial closing — a 12-item guided review checklist per period, frozen income/expense/investment-contribution/debt-payment totals plus a linked net-worth snapshot at completion, reopening with a required typed reason, versioned corrections that never rewrite a prior closing's frozen figures, and a monthly report (income/expenses/free cash flow/investment contribution/debt reduction/net-worth change/major unusual expenses/missed commitments/goal progress/upcoming obligations) that explicitly states when its data is incomplete at `/app/monthly-closing` (+ detail) — see the Monthly financial closing row above (PROMPT 33)
- [x] Documents — a household-wide vault at `/app/documents` with a 17-value category taxonomy, optional loose entity linking, document/expiry dates, active/archived status, and a client-computed checksum, stored in a private Storage bucket with signed-URL-only reads and owner/admin-only permanent deletion — see the Financial documents vault row above (PROMPT 34). Separate from `attachments` (PROMPT 9), which continues to serve the small set of per-entity upload widgets (transactions, insurance claims, assets) it always has
- [x] Reminders — a financial calendar at `/app/reminders` generating 12 reminder types from each source module's own dates (SIP due, EMI due, insurance premium, policy renewal, loan review, expected income, lending repayment, document expiry, fixed-deposit maturity, goal review, monthly closing, asset valuation review), with upcoming/overdue/snoozed/completed/skipped views, snooze/skip/complete/reopen, related-entity links, dedup, and timezone-correct due/overdue classification — see the Reminders / financial calendar row above (PROMPT 35)
- [x] Reports — a reporting centre at `/app/reports` (+ `/app/reports/[slug]`) covering all 17 named reports (cash flow, income, expenses, essential/discretionary, investments ×5, debt ×2, insurance, assets/liabilities, lending, goals, emergency fund, net worth), each with a stated question/date-range/data-cutoff, an accessible summary, empty-state handling, privacy-mode-concealed charts and tables, and CSV export — see the Reports / reporting centre row above (PROMPT 36). Reuses existing feature calculators/queries and 10 existing chart components rather than reimplementing; the one new metric (SIP consistency) is unit-tested in `src/lib/calculations/reports.ts`
- [x] Financial calculators — six standalone/account-linked what-if calculators (SIP projection, lump sum, daily growth, EMI, loan prepayment, goal funding) at `/app/calculators`, with an explicit-save-only scenario table — see the Financial calculators row above
- [x] Financial decision journal — one entry per significant financial decision at `/app/decisions` (+ detail), with a database-enforced immutability trigger preserving the original title/context/choice/alternatives/rationale/expected_result/risks forever (only status/review_date/actual_outcome/lessons_learned may change later), a closed trigger-verified link to the related account/SIP/loan/lending/asset/goal/insurance policy, review dates that generate a real reminder (extending PROMPT 35), and atomic, history-preserving superseding via an RPC mirroring `create_insurance_policy`'s renewal pattern — see the Financial decision journal row above (PROMPT 37)
- [ ] Financial-literacy explanations
- [ ] Data export
- [ ] Production deployment

## 3. Recommended implementation order

Sequenced so each phase is independently useful and later phases depend on earlier data existing, rather than building UI for data that can't exist yet:

1. **Foundation**: repo scaffold (Next.js + TypeScript + pnpm) ✅, environment variable setup ✅, base layout shell ✅, household + membership model with RLS (roles, status, idempotent creation) ✅, auth (sign up/in/out, verification, password reset, protected routes, onboarding that creates both the profile and the household, `profiles` table) ✅, authenticated app shell (navigation, mobile nav, privacy mode) ✅ — remaining: a real (hosted) Supabase project for staging/production, and a collaboration-invite flow to actually add a second person to an existing household.
2. **Core ledger**: institutions, accounts, transactions (income/expense/transfer discriminated), income sources, recurring commitments. Schema complete (`people`, `institutions`, `financial_accounts`, `account_balance_snapshots`, `transaction_categories`, `transactions`, `transaction_splits`, `recurring_rules`, `attachments`, `activity_events`, `income_sources` — see [database-plan.md](./database-plan.md), [financial-domain-model.md](./financial-domain-model.md)). **People, Institutions, Accounts, Categories, Transactions, Income, and Expenses now have full CRUD/analysis UI** (`/app/people`, `/app/institutions` — PROMPT 8; `/app/accounts` — PROMPT 9; `/app/cash-flow` + `/app/cash-flow/categories` — PROMPT 10; `/app/income` — PROMPT 11; `/app/expenses` — PROMPT 12), built first per this phase's own ordering since loans/policies/investments will all reference them. Recurring commitments (`recurring_rules`) still has no dedicated management UI or generation job — the Expenses feature can create a minimal rule when an expense is marked recurring, but nothing lets a household view, edit, or generate future occurrences from one yet.
3. **Net worth foundation**: assets (movable/immovable) ✅ (PROMPT 27/28), liabilities ✅ (PROMPT 24), valuation snapshots ✅ (`asset_valuation_snapshots`, PROMPT 27), net-worth snapshot rollup ✅ (PROMPT 32 — see the Net-worth engine row in section 1). **Deviation from this document's own recommended order**: this phase was originally meant to come *before* investments/debt/protection (phases 4–6), since a net-worth rollup needs something to sum even in minimal form — instead it was built last, well after every module it rolls up already existed independently. This turned out fine in practice: PROMPT 32's rollup was able to reuse every other module's own already-correct outstanding/valuation/ownership logic directly (loans' `getDebtSummary`, lending's `computeLendingTotals`, assets' `computeNetWorthContributionMinorUnits`, liabilities' certainty split) rather than needing any of them to change shape to accommodate it — the "teach a future rollup to read this" notes left in phases 4–6 below turned out to require no actual rework.
4. **Investing**: `investment_accounts`/`investment_assets`/`investment_holdings`/`investment_transactions`/`investment_valuation_snapshots`/`investment_documents` schema ✅ (PROMPT 16 — see [database-plan.md](./database-plan.md) §3, [financial-domain-model.md](./financial-domain-model.md) §4); SIPs ✅ (PROMPT 17), portfolio/performance reporting ✅ (PROMPT 18), and dedicated staking-position/daily-value tracking ✅ (PROMPT 19) all built — see the rows in section 1 above. **Deviation from this document's own recommended order**: PROMPT 16 asked for the investment domain directly, before phase 3 (net-worth foundation) existed — the investment tables were built without assets/liabilities/net-worth-rollup underneath them yet. This turned out fine structurally (investment_holdings' valuation history stood on its own) — PROMPT 32's net-worth rollup later read investment_holdings' latest valuations directly via `getPortfolioHoldings`, exactly as anticipated, with no changes needed to the investing domain itself.
5. **Debt**: loans (with principal/interest-split payments) ✅ (PROMPT 21 — see the Loans & debt row in section 1); lending/receivables ✅ (PROMPT 23 — see the Lending & receivables row in section 1); informal borrowing and general liabilities ✅ (PROMPT 24 — see the Informal borrowing & liabilities row in section 1), all reusing the same append-only payment-history pattern loans established. **Deviation from this document's own recommended order**: PROMPT 21/23/24 asked for loans, lending, and liabilities directly, ahead of phase 3 (net worth) and phase 7 (planning) — this turned out fine, since PROMPT 32's net-worth rollup later folded all three in directly (`getDebtSummary` for institutional debt, `computeLendingTotals` for receivables, `computeOtherLiabilitiesBreakdown` splitting liabilities by source/certainty) with no changes needed to any of the three modules themselves.
6. **Protection & property depth**: insurance policies ✅ (PROMPT 25 — see the Insurance row in section 1) plus claims, structured waiting periods, and a dashboard ✅ (PROMPT 26 — see the Insurance claims & dashboard row); movable/immovable/business assets ✅ (PROMPT 27 — see the Assets row), deepened with property-specific fields, valuation confidence, and asset-linked income ✅ (PROMPT 28 — see the Property depth & asset income row). Documents: `transaction` (Expenses' receipts), `insurance_claim`, and `asset` have real upload UI — `financial_account`/`lending`/`insurance_policy` remain schema-ready only. **Deviation from this document's own recommended order**: PROMPT 25/26/27/28 asked for insurance, claims, assets, and property depth directly, ahead of phase 3 (net worth) and phase 7 (planning) — `computeNetWorthContributionMinorUnits` (src/lib/calculations/assets.ts) was already the exact per-asset figure a future rollup would sum; PROMPT 32's net-worth rollup later summed it directly (split by asset_group into movable assets and property), with no changes needed to the assets module itself. A policy's coverage amount is still not folded into net worth — insurance coverage was never part of PROMPT 32's formula (it's protection against a future loss, not a currently-owned asset).
7. **Planning**: goals ✅ (PROMPT 30 — see the Financial goals row in section 1; built on top of the Accounts and Investments modules' own balance/valuation queries rather than a new value pipeline, and reusing PROMPT 20's `goal-funding.ts` calculator directly for the future-value math); emergency-fund planning ✅ (PROMPT 31 — see the Emergency fund planner row; built the same way, on top of Expenses/Loans/Insurance's own existing queries rather than a new value pipeline); monthly financial closing ✅ (PROMPT 33 — see the Monthly financial closing row; the capstone of this phase, needing steps 2–6's data to close against, plus the net-worth engine from earlier in this same phase — sequenced last within Planning specifically because it depends on the most other modules). **Deviation**: PROMPT 29 (money drains) was also built ahead of this phase — see item 8 below, since it's an analysis layer over existing modules (recurring commitments, assets) rather than something planning itself depends on.
8. **Money drains** (PROMPT 29 — see the Money drains row in section 1): a household-entered register of depreciating and money-draining items, cross-linking to `recurring_rules` and `assets` rather than duplicating either. Not part of this document's original numbered phases (it isn't in [product-scope.md](./product-scope.md)'s module list) — sequenced here because it reads/references data every earlier phase already produces (recurring commitments from phase 2, assets from phase 6) without needing net worth or planning to exist first.
9. **Documents vault** (PROMPT 34 — see the Financial documents vault row in section 1): a household-wide document store with its own category taxonomy, expiry tracking, and archive/permanent-delete lifecycle, reusing PROMPT 9's private Storage bucket and RLS unchanged. Not part of this document's original numbered phases — sequenced here, after every module that might want to link a document to one of its records (accounts, loans, insurance, assets, goals, ...) already exists, but before the insight layer, since "documents" is explicitly named as its own line item in [product-scope.md](./product-scope.md)'s module list, unlike money drains.
10. **Reminders / financial calendar** (PROMPT 35 — see the Reminders / financial calendar row in section 1): generated occurrences of dated obligations across essentially every other module. Sequenced here, after every source module it reads from already exists (SIPs, loans, insurance, income, lending, documents, accounts, goals, assets, monthly closing) — an "insight layer" item in [product-scope.md](./product-scope.md)'s original module list, but pulled ahead of reports/decision-journal/literacy (item 11 below) since it's the one insight-layer item that needed the most other modules to be real first, not the least.
11. **Reports / reporting centre** (PROMPT 36 — see the Reports / reporting centre row in section 1): 17 reports spanning cash flow, investments, debt, protection/property, lending, planning, and net worth, sequenced last within the insight layer specifically because a report is only as good as the modules it reads from — by this point in the build, every one of them already existed. **Deviation from this document's own recommended order**: reports/reminders/decision-journal/literacy were originally meant to be one undifferentiated "insight layer" item (12 below); PROMPT 35 and PROMPT 36 split reminders and reports out and built them back-to-back instead, in the reverse of product-scope.md's own listed order (reports before reminders) — reports turned out to depend on reminders for nothing at all, so the actual build order made no structural difference, only a documentation one.
12. **Financial decision journal** (PROMPT 37 — see the Financial decision journal row in section 1): captures the reasoning behind a significant financial decision at the time it's made, immutably. Sequenced last of the three insight-layer splits (reminders, reports, decision journal) — it extends PROMPT 35's reminders (review dates) the same way reports read from every other module, so it needed both to already exist.
13. **Insight layer**: reports ✅ (PROMPT 36 — see item 11 above); projections/calculators (with assumption disclosure) ✅ (PROMPT 20 — see the Financial calculators row in section 1); reminders ✅ (PROMPT 35 — see item 10 above); financial decision journal ✅ (PROMPT 37 — see item 12 above); financial-literacy content not started. **Deviation from this document's own recommended order**: PROMPT 20 asked for calculators directly, ahead of reports/reminders/decision journal/literacy in this same phase and well ahead of phases 3, 5, 6, and 7 (net worth, debt, insurance/assets, goals) that a loan-prepayment or goal-funding calculator would eventually want to read real data from. Every PROMPT 20 calculator works fully standalone today (manually-entered inputs only); the three account-linked ones (lump sum, daily growth, goal funding) read an existing `financial_accounts` balance to prefill, but none of them read a real loan or goal record yet, since those modules don't exist — a future Loans/Goals module should teach those calculators to link to a real record the same way, rather than the calculators needing to change.
14. **Platform hardening**: data export, deployment pipeline, accessibility and security pass across everything built, performance/testing hardening.

Rationale for this order: it front-loads the two things every later feature depends on — tenancy/security (phase 1) and the append-only historical-record pattern (phases 3–5) — so those patterns are established once and reused, rather than retrofitted after multiple modules already violate them.

## 4. How to keep this document current

Update section 1 and check off section 2 as each module is actually built and tested (not merely started), and note any deviation from the recommended order in section 3 with a one-line reason.
